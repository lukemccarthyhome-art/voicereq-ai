<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login — Morti</title>
    <style>
        :root {
            --brand: #4f46e5; --brand-light: #6366f1; --brand-dark: #3730a3;
            --accent: #10b981; --text: #1e293b; --text-mid: #475569; --text-light: #64748b;
            --bg: #ffffff; --bg-alt: #f8fafc; --border: #e2e8f0;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; color: var(--text); background: var(--bg-alt); min-height: 100vh; }
        a { text-decoration: none; color: inherit; }

        .login-container { max-width: 440px; margin: 140px auto 60px; padding: 0 20px; }
        .login-card { background: var(--bg); border-radius: 16px; padding: 48px 40px; box-shadow: 0 2px 16px rgba(0,0,0,0.06); border: 1px solid var(--border); }
        .login-card .logo { text-align: center; margin-bottom: 32px; }
        .login-card .logo h1 { font-size: 1.5rem; color: var(--text); display: flex; align-items: center; justify-content: center; gap: 8px; font-weight: 800; }
        .login-card .logo h1 span { color: var(--brand); }
        .login-card .logo p { color: var(--text-light); margin-top: 8px; font-size: 0.9rem; }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; font-size: 0.9rem; font-weight: 600; color: var(--text-mid); margin-bottom: 6px; }
        .form-control {
            width: 100%; padding: 12px 16px; border: 2px solid var(--border);
            border-radius: 10px; font-size: 0.95rem; font-family: inherit; transition: border-color 0.2s;
        }
        .form-control:focus { outline: none; border-color: var(--brand); }
        .btn-submit {
            display: inline-block; padding: 14px 36px; border-radius: 10px;
            font-weight: 600; font-size: 1rem; border: none; cursor: pointer;
            background: var(--brand); color: #fff; box-shadow: 0 2px 8px rgba(79,70,229,0.3);
            transition: all 0.2s; width: 100%;
        }
        .btn-submit:hover { background: var(--brand-light); transform: translateY(-1px); }
        .alert-error { background: #fff5f5; border: 2px solid #fed7d7; border-radius: 8px; padding: 12px 16px; color: #c53030; margin-bottom: 20px; font-size: 0.9rem; text-align: center; }
        .login-footer { text-align: center; margin-top: 24px; color: var(--text-light); font-size: 0.9rem; }
        .login-footer a { color: var(--brand); font-weight: 600; }

        footer { text-align: center; padding: 40px; color: var(--text-light); font-size: 0.85rem; border-top: 1px solid var(--border); }
        footer a { color: var(--brand); }

        @media (max-width: 768px) {
            .login-container { margin-top: 80px; }
            .login-card { padding: 32px 24px; }
        }
    </style>
</head>
<body>

<%- include('partials/marketing-nav', { currentPage: 'login' }) %>

<div class="login-container">
    <div class="login-card">
        <div class="logo">
            <h1><span>◆</span> Morti</h1>
            <p>Sign in to your account</p>
        </div>

        <% if (error) { %>
            <div class="alert-error"><%= error %></div>
        <% } %>

        <form method="POST" action="/login">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" class="form-control" required value="<%= email || '' %>">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-control" required>
            </div>
            <button type="submit" class="btn-submit">Sign In</button>
        </form>

        <div class="login-footer">
            Don't have an account? <a href="/signup">Sign up</a>
        </div>
    </div>
</div>

<footer>
    <p>&copy; <%= new Date().getFullYear() %> <a href="https://morti.com.au">Morti Pty Ltd</a> · Australia &nbsp;·&nbsp; <a href="/">Home</a> &nbsp;·&nbsp; <a href="/contact">Contact</a></p>
</footer>

</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login — Morti</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #050d1a;
            --bg-primary: #0a1628;
            --bg-card: #0f1d32;
            --bg-glass: rgba(15, 29, 50, 0.6);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa;
            --accent-glow: rgba(17, 153, 250, 0.4);
            --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #009e7e;
            --accent-purple: #8b5cf6;
            --text-primary: #f0f4f8;
            --text-secondary: rgba(240, 244, 248, 0.7);
            --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px;
            --radius-pill: 50px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: var(--text-primary); background: var(--bg-deep);
            overflow-x: hidden; font-weight: 400; min-height: 100vh; display: flex; flex-direction: column;
            -webkit-font-smoothing: antialiased;
        }
        a { text-decoration: none; color: inherit; }

        /* ========== NAV ========== */
        nav {
            position: fixed; top: 0; width: 100%; z-index: 100;
            display: flex; align-items: center; justify-content: space-between;
            padding: 16px 40px;
            background: rgba(5, 13, 26, 0.8);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-glass); transition: all 0.3s;
        }
        nav.scrolled { background: rgba(5, 13, 26, 0.95); }
        .nav-brand { font-size: 1.3rem; font-weight: 800; color: #fff; letter-spacing: -0.02em; }
        .nav-links { display: flex; gap: 32px; align-items: center; }
        .nav-links a.nav-text { font-size: 0.88rem; color: var(--text-muted); transition: color 0.2s; font-weight: 500; letter-spacing: 0.01em; }
        .nav-links a.nav-text:hover { color: #fff; }
        .nav-links a.nav-text.active { color: var(--accent); }
        .nav-cta {
            background: var(--accent); color: #fff; padding: 10px 28px;
            border-radius: var(--radius-pill); font-size: 0.88rem; font-weight: 600;
            box-shadow: 0 0 16px var(--accent-glow); transition: all 0.3s;
        }
        .nav-cta:hover { background: #2aa8ff; box-shadow: 0 0 24px var(--accent-glow); transform: translateY(-1px); }
        /* Nav dropdown */
        .nav-dropdown { position: relative; }
        .nav-dropdown-trigger { cursor: pointer; }
        .nav-arrow { font-size: 0.7em; margin-left: 2px; transition: transform 0.2s; display: inline-block; }
        .nav-dropdown:hover .nav-arrow { transform: rotate(180deg); }
        .nav-dropdown-menu {
            position: absolute; top: calc(100% + 12px); left: 50%; transform: translateX(-50%);
            min-width: 220px; padding: 8px;
            background: rgba(10, 22, 40, 0.95);
            backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-glass); border-radius: 12px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.4);
            opacity: 0; visibility: hidden; transition: opacity 0.2s, visibility 0.2s; z-index: 200;
        }
        .nav-dropdown:hover .nav-dropdown-menu { opacity: 1; visibility: visible; }
        .nav-dropdown-item {
            display: block; padding: 10px 16px; font-size: 0.88rem; font-weight: 500;
            color: var(--text-muted); border-radius: 8px; transition: all 0.2s;
        }
        .nav-dropdown-item:hover { color: #fff; background: rgba(255,255,255,0.06); }
        .hamburger { display: none; background: none; border: none; font-size: 1.5rem; cursor: pointer; padding: 4px 8px; color: #fff; }
        @media (max-width: 768px) {
            nav { padding: 12px 20px; }
            .hamburger { display: block; }
            .nav-links {
                display: none; flex-direction: column; position: absolute; top: 100%;
                left: 0; right: 0; background: rgba(10, 22, 40, 0.98);
                backdrop-filter: blur(20px); padding: 16px 20px; gap: 4px;
                border-bottom: 1px solid var(--border-glass);
            }
            .nav-links.open { display: flex; }
            .nav-links a.nav-text { padding: 12px 0; font-size: 1rem; }
            .nav-cta { text-align: center; margin-top: 8px; display: block; }
            .nav-dropdown-menu {
                position: static; transform: none; opacity: 1; visibility: visible;
                background: transparent; border: none; box-shadow: none;
                backdrop-filter: none; padding: 0 0 0 16px; display: none;
            }
            .nav-dropdown.open .nav-dropdown-menu { display: block; }
            .nav-dropdown-trigger { padding: 12px 0; font-size: 1rem; }
        }

        /* ========== LOGIN LAYOUT ========== */
        .login-section {
            flex: 1; display: flex; align-items: center; justify-content: center;
            padding: 140px 24px 80px; position: relative; overflow: hidden;
        }
        .login-section::before {
            content: ''; position: absolute; width: 500px; height: 500px;
            background: rgba(17, 153, 250, 0.08); border-radius: 50%;
            filter: blur(100px); top: 10%; right: -10%; pointer-events: none;
        }
        .login-section::after {
            content: ''; position: absolute; width: 400px; height: 400px;
            background: rgba(139, 92, 246, 0.06); border-radius: 50%;
            filter: blur(100px); bottom: 10%; left: -5%; pointer-events: none;
        }
        .login-inner {
            display: grid; grid-template-columns: 1fr 1fr; gap: 80px;
            max-width: 960px; width: 100%; align-items: center; position: relative; z-index: 1;
        }
        .login-hero h1 {
            font-size: clamp(2rem, 3.5vw, 2.8rem); font-weight: 900;
            letter-spacing: -0.04em; line-height: 1.1; margin-bottom: 20px;
        }
        .login-hero .gradient-text {
            background: linear-gradient(135deg, var(--accent), var(--accent-secondary));
            -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
        }
        .login-hero p { color: var(--text-secondary); font-size: 1.05rem; line-height: 1.8; font-weight: 300; }
        .login-card {
            background: rgba(15, 29, 50, 0.85); border-radius: var(--radius); padding: 48px 40px;
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
        }
        .login-card .logo { text-align: center; margin-bottom: 32px; }
        .login-card .logo h1 { font-size: 1.5rem; color: var(--text-primary); display: flex; align-items: center; justify-content: center; gap: 8px; font-weight: 800; }
        .login-card .logo h1 span { color: var(--accent); }
        .login-card .logo p { color: var(--text-muted); margin-top: 8px; font-size: 0.9rem; }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; font-size: 0.9rem; font-weight: 600; color: #f0f4f8; margin-bottom: 6px; }
        .form-control {
            width: 100%; padding: 12px 16px; border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 10px; font-size: 0.95rem; font-family: inherit; transition: all 0.25s;
            background: rgba(10, 22, 40, 0.8); color: #f0f4f8;
        }
        .form-control::placeholder { color: var(--text-muted); }
        .form-control:focus {
            outline: none; border-color: var(--accent);
            box-shadow: 0 0 0 3px var(--accent-dim);
        }
        .btn-submit {
            display: inline-block; padding: 14px 36px; border-radius: var(--radius-pill);
            font-weight: 600; font-size: 1rem; border: none; cursor: pointer;
            background: var(--accent); color: #fff;
            box-shadow: 0 0 20px var(--accent-glow), 0 4px 12px rgba(0, 0, 0, 0.3);
            transition: all 0.3s; width: 100%;
        }
        .btn-submit:hover { background: #2aa8ff; transform: translateY(-2px); box-shadow: 0 0 32px var(--accent-glow), 0 8px 24px rgba(0, 0, 0, 0.4); }
        .alert-error {
            background: rgba(239, 68, 68, 0.1); border: 1px solid rgba(239, 68, 68, 0.3);
            border-radius: 8px; padding: 12px 16px; color: #f87171; margin-bottom: 20px; font-size: 0.9rem; text-align: center;
        }
        .login-footer { text-align: center; margin-top: 24px; color: var(--text-muted); font-size: 0.9rem; }
        .login-footer a { color: var(--accent); font-weight: 600; }

        /* ========== FOOTER ========== */
        .site-footer {
            background: var(--bg-deep); color: var(--text-muted); padding: 72px 24px 40px;
            border-top: 1px solid var(--border-glass);
        }
        .footer-grid {
            display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 48px; max-width: 1000px; margin: 0 auto 48px;
        }
        .footer-brand { font-size: 1.3rem; font-weight: 800; color: #fff; margin-bottom: 12px; }
        .footer-brand-desc { font-size: 0.88rem; line-height: 1.8; color: var(--text-muted); max-width: 280px; }
        .footer-col h4 { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.14em; color: var(--text-muted); font-weight: 600; margin-bottom: 16px; }
        .footer-col a { display: block; font-size: 0.88rem; color: var(--text-muted); padding: 5px 0; transition: color 0.2s; }
        .footer-col a:hover { color: #fff; }
        .footer-bottom {
            text-align: center; padding-top: 32px; border-top: 1px solid var(--border-glass);
            font-size: 0.82rem; color: var(--text-muted); max-width: 1000px; margin: 0 auto;
        }

        @media (max-width: 768px) {
            .login-inner { grid-template-columns: 1fr; gap: 40px; }
            .login-hero { text-align: center; }
            .login-section { padding: 100px 20px 60px; }
            .login-card { padding: 32px 24px; }
            .footer-grid { grid-template-columns: 1fr 1fr; gap: 32px; }
        }
        @media (max-width: 480px) { .footer-grid { grid-template-columns: 1fr; gap: 24px; } }
    </style>
</head>
<body>

<nav>
    <a href="/" class="nav-brand">◆ Morti</a>
    <button class="hamburger" onclick="this.nextElementSibling.classList.toggle('open')" aria-label="Menu">☰</button>
    <div class="nav-links">
        <div class="nav-dropdown">
            <a href="/#products" class="nav-text nav-dropdown-trigger<%= typeof currentPage !== 'undefined' && (currentPage === 'products' || currentPage === 'ai-readiness') ? ' active' : '' %>">Products <span class="nav-arrow">▾</span></a>
            <div class="nav-dropdown-menu">
                <a href="/#products" class="nav-dropdown-item">Morti — Talk, Automate</a>
                <a href="/ai-readiness" class="nav-dropdown-item">AI Readiness Review</a>
            </div>
        </div>
        <a href="/about" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'about' ? ' active' : '' %>">About</a>
        <a href="/#pricing" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'pricing' ? ' active' : '' %>">Pricing</a>
        <a href="/contact" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'contact' ? ' active' : '' %>">Contact</a>
        <a href="/login" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'login' ? ' active' : '' %>">Login</a>
        <a href="/signup" class="nav-cta">Get Started</a>
    </div>
</nav>

<section class="login-section">
    <div class="login-inner">
        <div class="login-hero">
            <h1>Welcome<br><span class="gradient-text">back.</span></h1>
            <p>Sign in to manage your projects, track progress, and keep your automations running smoothly.</p>
        </div>
        <div class="login-card">
            <div class="logo">
                <h1><span>◆</span> Morti</h1>
                <p>Sign in to your account</p>
            </div>

            <% if (error) { %>
                <div class="alert-error"><%= error %></div>
            <% } %>

            <form method="POST" action="/login">
                <div class="form-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" class="form-control" required value="<%= email || '' %>">
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" class="form-control" required>
                </div>
                <button type="submit" class="btn-submit">Sign In</button>
            </form>

            <div style="display:flex;align-items:center;gap:12px;margin:20px 0;">
                <div style="flex:1;height:1px;background:rgba(255,255,255,0.15);"></div>
                <span style="color:var(--text-muted);font-size:0.82rem;font-weight:500;">or</span>
                <div style="flex:1;height:1px;background:rgba(255,255,255,0.15);"></div>
            </div>

            <a href="/auth/google" style="display:flex;align-items:center;justify-content:center;gap:10px;padding:12px 20px;border-radius:12px;background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.15);color:var(--text-primary);font-weight:600;font-size:0.92rem;text-decoration:none;transition:all 0.2s ease;cursor:pointer;" onmouseover="this.style.background='rgba(255,255,255,0.12)';this.style.borderColor='rgba(255,255,255,0.25)'" onmouseout="this.style.background='rgba(255,255,255,0.08)';this.style.borderColor='rgba(255,255,255,0.15)'">
                <svg width="20" height="20" viewBox="0 0 24 24"><path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92a5.06 5.06 0 01-2.2 3.32v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.1z" fill="#4285F4"/><path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/><path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/><path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/></svg>
                Continue with Google
            </a>

            <% if (typeof success !== 'undefined' && success) { %>
                <div style="margin-top:16px;padding:12px 16px;border-radius:12px;background:rgba(0,158,126,0.12);border:1px solid rgba(0,158,126,0.25);color:#009e7e;font-size:0.88rem;font-weight:500;"><%= success %></div>
            <% } %>

            <div class="login-footer">
                Don't have an account? <a href="/signup">Sign up</a>
            </div>
        </div>
    </div>
</section>

<footer class="site-footer">
    <div class="footer-grid">
        <div>
            <div class="footer-brand">◆ Morti</div>
            <p class="footer-brand-desc">You talk, we build. AI-powered project delivery and managed automation for Australian small and medium businesses.</p>
        </div>
        <div class="footer-col">
            <h4>Product</h4>
            <a href="/#how-it-works">How It Works</a>
            <a href="/#pricing">Pricing</a>
            <a href="/#products">Products</a>
        </div>
        <div class="footer-col">
            <h4>Company</h4>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <a href="/signup">Get Started</a>
            <a href="/login">Customer Login</a>
        </div>
        <div class="footer-col">
            <h4>Contact</h4>
            <a href="mailto:info@morti.com.au">Email Us</a>
            <span style="display:block;font-size:0.88rem;padding:5px 0;">Australia</span>
            <a href="https://morti.com.au">morti.com.au</a>
        </div>
    </div>
    <div class="footer-bottom">
        &copy; <%= new Date().getFullYear() %> Morti Pty Ltd · ABN 15 687 741 797 · Australia
    </div>
</footer>

<script>
window.addEventListener('scroll', () => {
    const nav = document.querySelector('nav');
    if (nav) nav.classList.toggle('scrolled', window.scrollY > 20);
});
document.querySelectorAll('.nav-dropdown-trigger').forEach(trigger => {
    trigger.addEventListener('click', function(e) {
        if (window.innerWidth <= 768) { e.preventDefault(); this.closest('.nav-dropdown').classList.toggle('open'); }
    });
});
</script>

</body>
</html>
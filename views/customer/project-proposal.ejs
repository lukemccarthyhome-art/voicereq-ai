<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.name %> - Proposal</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f0f2f5; line-height: 1.6; }
        .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 16px 0; }
        .header-container { max-width: 1200px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; padding: 0 20px; }
        .logo { font-size: 20px; font-weight: bold; text-decoration: none; color: white; }
        .nav { display: flex; gap: 20px; align-items: center; }
        .nav a { color: rgba(255,255,255,0.9); text-decoration: none; padding: 8px 14px; border-radius: 6px; transition: background 0.2s; font-weight: 500; }
        .nav a:hover { background: rgba(255,255,255,0.2); }
        .main { max-width: 1000px; margin: 0 auto; padding: 20px; }
        .card { background: white; border-radius: 12px; padding: 24px; margin-bottom: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
        .btn { display: inline-block; padding: 10px 20px; background: #667eea; color: white; text-decoration: none; border-radius: 8px; font-weight: 600; border: none; cursor: pointer; font-size: 14px; }
        .btn:hover { opacity: 0.9; }
        .breadcrumb { font-size: 13px; color: #666; margin-bottom: 20px; }
        .breadcrumb a { color: #667eea; text-decoration: none; }

        .fee-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin: 20px 0; }
        .fee-card { background: #f8fafc; border: 2px solid #e2e8f0; border-radius: 12px; padding: 24px; text-align: center; }
        .fee-card.highlight { border-color: #667eea; background: #f0f4ff; }
        .fee-label { font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #667eea; font-weight: 700; margin-bottom: 8px; }
        .fee-amount { font-size: 2rem; font-weight: 800; color: #1e293b; margin-bottom: 8px; }
        .fee-amount small { font-size: 0.5em; color: #64748b; font-weight: 400; }
        .fee-desc { font-size: 0.85rem; color: #64748b; line-height: 1.5; }

        .total-bar { background: linear-gradient(135deg, #667eea, #764ba2); color: white; border-radius: 12px; padding: 24px; display: flex; justify-content: space-around; text-align: center; margin: 20px 0; }
        .total-item h4 { font-size: 0.8rem; text-transform: uppercase; letter-spacing: 1px; opacity: 0.8; margin-bottom: 4px; }
        .total-item .amount { font-size: 1.5rem; font-weight: 800; }

        .value-card { background: #eff6ff; border: 2px solid #bfdbfe; border-radius: 12px; padding: 24px; margin: 20px 0; }
        .value-card h3 { color: #1e40af; margin-bottom: 16px; }
        .value-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #dbeafe; }
        .value-row:last-child { border: none; }
        .value-label { color: #1e40af; font-size: 0.9rem; }
        .value-text { color: #334155; font-size: 0.9rem; max-width: 60%; text-align: right; }
        .scenario-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-top: 16px; }
        .scenario { text-align: center; padding: 16px; border-radius: 8px; background: white; }
        .scenario-label { font-size: 0.75rem; text-transform: uppercase; color: #64748b; letter-spacing: 0.5px; }
        .scenario-amount { font-size: 1.3rem; font-weight: 800; color: #1e40af; margin-top: 4px; }

        .roi-card { background: #f0fdf4; border: 2px solid #bbf7d0; border-radius: 12px; padding: 24px; margin: 20px 0; }
        .roi-card h3 { color: #166534; margin-bottom: 12px; }
        .roi-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin-bottom: 16px; }
        .roi-stat { text-align: center; padding: 16px; background: white; border-radius: 8px; }
        .roi-stat .label { font-size: 0.75rem; text-transform: uppercase; color: #166534; letter-spacing: 0.5px; }
        .roi-stat .val { font-size: 1.5rem; font-weight: 800; color: #166534; margin-top: 4px; }
        .roi-summary { color: #166534; font-size: 0.95rem; font-style: italic; padding-top: 12px; border-top: 1px solid #dcfce7; }

        .section { margin-bottom: 20px; }
        .section h3 { font-size: 1.1rem; color: #334155; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
        .section p { color: #475569; font-size: 0.95rem; line-height: 1.7; white-space: pre-wrap; }
        .tag-list { display: flex; flex-wrap: wrap; gap: 8px; }
        .tag { background: #f1f5f9; padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; color: #475569; }
        .tag.excl { background: #fef2f2; color: #991b1b; }

        .approve-card { background: linear-gradient(135deg, #f0fdf4, #dcfce7); border: 2px solid #86efac; border-radius: 12px; padding: 32px; text-align: center; margin: 24px 0; }
        .approve-card h3 { color: #166534; margin-bottom: 8px; }
        .approve-card p { color: #15803d; margin-bottom: 16px; font-size: 0.95rem; }
        .btn-approve { background: #16a34a; color: white; padding: 14px 32px; font-size: 16px; font-weight: 700; border: none; border-radius: 10px; cursor: pointer; }
        .btn-approve:hover { background: #15803d; }

        .approved-banner { background: #d1fae5; border: 2px solid #86efac; border-radius: 12px; padding: 20px; text-align: center; margin: 20px 0; }
        .approved-banner h3 { color: #065f46; }

        @media (max-width: 768px) {
            .fee-grid, .scenario-grid, .roi-grid { grid-template-columns: 1fr; }
            .total-bar { flex-direction: column; gap: 16px; }
            .value-row { flex-direction: column; gap: 4px; }
            .value-text { max-width: 100%; text-align: left; }
        }
    </style>
</head>
<body>

<div class="header">
    <div class="header-container">
        <a href="/dashboard" class="logo">🎙️ Morti Projects</a>
        <nav class="nav">
            <a href="/dashboard">Dashboard</a>
            <a href="/projects">Projects</a>
            <a href="/profile">Profile</a>
            <a href="/logout">🚪 Logout</a>
        </nav>
    </div>
</div>

<div class="main">
    <div class="breadcrumb">
        <a href="/dashboard">Dashboard</a> → <a href="/projects">Projects</a> → <a href="/projects/<%= project.id %>"><%= project.name %></a> → Proposal
    </div>

    <% if (proposal.approvedAt) { %>
    <div class="approved-banner">
        <h3>✅ Proposal Approved</h3>
        <p style="color: #065f46; margin-top: 4px;">Approved by <%= proposal.approvedBy %> on <%= new Date(proposal.approvedAt).toLocaleDateString() %></p>
    </div>
    <% } %>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
        <h2>💰 Proposal — <%= project.name %></h2>
        <a href="/projects/<%= project.id %>" class="btn" style="background: #64748b;">← Back to Project</a>
    </div>

    <!-- Header -->
    <div class="card">
        <h3 style="margin-bottom: 4px;"><%= proposal.projectName || project.name %></h3>
        <div style="color: #64748b; font-size: 0.9rem;">
            Client: <strong><%= proposal.clientCompany || 'TBD' %></strong>
            &nbsp;·&nbsp; Prepared: <%= new Date(proposal.createdAt).toLocaleDateString() %>
        </div>

        <% if (proposal.commercialContext) { %>
        <div class="section" style="margin-top: 16px;">
            <h3>🏢 Commercial Context</h3>
            <p><%= proposal.commercialContext %></p>
        </div>
        <% } %>
    </div>

    <!-- Estimated Value -->
    <% if (proposal.estimatedValue) { %>
    <div class="value-card">
        <h3>💎 Estimated Value Created</h3>
        <% if (proposal.estimatedValue.annualSavings) { %>
        <div class="value-row">
            <span class="value-label">Annual Cost Savings</span>
            <span class="value-text"><%= proposal.estimatedValue.annualSavings %></span>
        </div>
        <% } %>
        <% if (proposal.estimatedValue.revenueUplift && proposal.estimatedValue.revenueUplift !== 'N/A') { %>
        <div class="value-row">
            <span class="value-label">Revenue Uplift</span>
            <span class="value-text"><%= proposal.estimatedValue.revenueUplift %></span>
        </div>
        <% } %>
        <% if (proposal.estimatedValue.efficiencyGains) { %>
        <div class="value-row">
            <span class="value-label">Efficiency Gains</span>
            <span class="value-text"><%= proposal.estimatedValue.efficiencyGains %></span>
        </div>
        <% } %>
        <% if (proposal.estimatedValue.strategicValue) { %>
        <div class="value-row">
            <span class="value-label">Strategic Value</span>
            <span class="value-text"><%= proposal.estimatedValue.strategicValue %></span>
        </div>
        <% } %>
        <% if (proposal.estimatedValue.conservativeAnnual || proposal.estimatedValue.moderateAnnual || proposal.estimatedValue.optimisticAnnual) { %>
        <div class="scenario-grid">
            <div class="scenario">
                <div class="scenario-label">Conservative</div>
                <div class="scenario-amount">$<%= (proposal.estimatedValue.conservativeAnnual || 0).toLocaleString() %></div>
            </div>
            <div class="scenario" style="background: #eff6ff;">
                <div class="scenario-label">Moderate</div>
                <div class="scenario-amount">$<%= (proposal.estimatedValue.moderateAnnual || 0).toLocaleString() %></div>
            </div>
            <div class="scenario">
                <div class="scenario-label">Optimistic</div>
                <div class="scenario-amount">$<%= (proposal.estimatedValue.optimisticAnnual || 0).toLocaleString() %></div>
            </div>
        </div>
        <% } %>
    </div>
    <% } %>

    <!-- Pricing Logic -->
    <% if (proposal.pricingLogic) { %>
    <div class="card">
        <div class="section">
            <h3>🧮 Pricing Logic</h3>
            <p><%= proposal.pricingLogic %></p>
        </div>
    </div>
    <% } %>

    <!-- Fee Cards -->
    <% if (proposal.fees) { %>
    <div class="fee-grid">
        <div class="fee-card">
            <div class="fee-label">Discovery & Design</div>
            <div class="fee-amount">$<%= (proposal.fees.discovery.amount || 0).toLocaleString() %></div>
            <div class="fee-desc"><%= proposal.fees.discovery.description %></div>
        </div>
        <div class="fee-card highlight">
            <div class="fee-label">Implementation</div>
            <div class="fee-amount">$<%= (proposal.fees.implementation.amount || 0).toLocaleString() %></div>
            <div class="fee-desc"><%= proposal.fees.implementation.description %></div>
        </div>
        <div class="fee-card">
            <div class="fee-label">Monthly Support</div>
            <div class="fee-amount">$<%= (proposal.fees.monthly_maintenance.amount || 0).toLocaleString() %> <small>/mo</small></div>
            <div class="fee-desc"><%= proposal.fees.monthly_maintenance.description %></div>
        </div>
    </div>
    <% } %>

    <!-- Totals -->
    <div class="total-bar">
        <div class="total-item">
            <h4>Total Upfront</h4>
            <div class="amount">$<%= (proposal.totalUpfront || 0).toLocaleString() %></div>
        </div>
        <div class="total-item">
            <h4>Annual Maintenance</h4>
            <div class="amount">$<%= (proposal.annualMaintenance || 0).toLocaleString() %> <small>/yr</small></div>
        </div>
        <div class="total-item">
            <h4>Timeline</h4>
            <div class="amount" style="font-size: 1.2rem;"><%= proposal.timeline %></div>
        </div>
    </div>

    <!-- Performance Components -->
    <% if (proposal.performanceComponents && proposal.performanceComponents !== 'None recommended for this engagement') { %>
    <div class="card">
        <div class="section">
            <h3>🎯 Performance-Linked Components</h3>
            <p><%= proposal.performanceComponents %></p>
        </div>
    </div>
    <% } %>

    <!-- ROI -->
    <% const roi = proposal.roiIllustration || proposal.roi; %>
    <% if (roi) { %>
    <div class="roi-card">
        <h3>📈 ROI Illustration</h3>
        <div class="roi-grid">
            <% if (roi.roiMultiple) { %>
            <div class="roi-stat">
                <div class="label">ROI Multiple</div>
                <div class="val"><%= roi.roiMultiple %></div>
            </div>
            <% } %>
            <% if (roi.paybackPeriod) { %>
            <div class="roi-stat">
                <div class="label">Payback Period</div>
                <div class="val"><%= roi.paybackPeriod %></div>
            </div>
            <% } %>
        </div>
        <% if (roi.summary) { %>
        <div class="roi-summary"><%= roi.summary %></div>
        <% } %>
    </div>
    <% } %>

    <!-- Sensitivity -->
    <% if (proposal.sensitivity) { %>
    <div class="card">
        <div class="section">
            <h3>📊 Sensitivity</h3>
            <p><%= proposal.sensitivity %></p>
        </div>
    </div>
    <% } %>

    <!-- Assumptions & Exclusions -->
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
        <div class="card">
            <h3 style="margin-bottom: 12px;">📌 Assumptions</h3>
            <div class="tag-list">
                <% (proposal.assumptions || []).forEach(a => { %>
                    <span class="tag"><%= a %></span>
                <% }) %>
            </div>
        </div>
        <div class="card">
            <h3 style="margin-bottom: 12px;">🚫 Exclusions</h3>
            <div class="tag-list">
                <% (proposal.exclusions || []).forEach(e => { %>
                    <span class="tag excl"><%= e %></span>
                <% }) %>
            </div>
        </div>
    </div>

    <!-- Approve -->
    <% if (!proposal.approvedAt) { %>
    <div class="approve-card">
        <h3>Ready to proceed?</h3>
        <p>By approving this proposal, you confirm acceptance of the scope, fees, and timeline outlined above.</p>
        <form method="POST" action="/customer/projects/<%= project.id %>/proposal/approve" onsubmit="return confirm('Approve this proposal? This confirms your acceptance of the scope and fees.')">
            <button type="submit" class="btn-approve">✅ Approve Proposal</button>
        </form>
    </div>
    <% } %>

    <div class="card" style="text-align: center; color: #94a3b8; font-size: 0.85rem;">
        Valid until: <%= proposal.validUntil || '30 days from generation' %>
    </div>
</div>

</body>
</html>

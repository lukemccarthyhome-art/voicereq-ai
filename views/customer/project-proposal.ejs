<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.name %> - Proposal</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #0a1628; --bg-primary: #0f1d32; --bg-card: #142640;
            --bg-glass: rgba(15, 29, 50, 0.6); --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa; --accent-glow: rgba(17, 153, 250, 0.4); --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #009e7e; --accent-purple: #8b5cf6;
            --text-primary: #f0f4f8; --text-secondary: rgba(240, 244, 248, 0.7); --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px; --radius-pill: 50px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif; background: var(--bg-deep); color: var(--text-primary); line-height: 1.6; -webkit-font-smoothing: antialiased; }
        a { text-decoration: none; color: inherit; }

        .header { background: rgba(5, 13, 26, 0.8); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border-bottom: 1px solid var(--border-glass); padding: 16px 0; }
        .header-container { max-width: 1200px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; padding: 0 24px; }
        .logo { font-size: 1.3rem; font-weight: 800; color: #fff; letter-spacing: -0.02em; }
        .nav { display: flex; gap: 8px; align-items: center; }
        .nav a { color: var(--text-muted); font-size: 0.88rem; padding: 8px 14px; border-radius: var(--radius-pill); transition: all 0.2s; font-weight: 500; }
        .nav a:hover { color: #fff; background: rgba(255,255,255,0.06); }

        .main { max-width: 1000px; margin: 0 auto; padding: 24px; }
        .card { background: var(--bg-glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 1px solid var(--border-glass); border-radius: var(--radius); padding: 24px; margin-bottom: 20px; transition: border-color 0.3s; }
        .card:hover { border-color: var(--border-glass-hover); }
        .btn { display: inline-flex; align-items: center; justify-content: center; padding: 10px 24px; border-radius: var(--radius-pill); font-size: 0.88rem; font-weight: 600; cursor: pointer; transition: all 0.3s; border: none; color: #fff; font-family: 'Inter', sans-serif; }
        .breadcrumb { font-size: 0.82rem; color: var(--text-muted); margin-bottom: 20px; }
        .breadcrumb a { color: var(--accent); }

        .fee-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin: 20px 0; }
        .fee-card { background: var(--bg-glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px); border: 2px solid var(--border-glass); border-radius: var(--radius); padding: 24px; text-align: center; transition: all 0.3s; }
        .fee-card:hover { border-color: var(--border-glass-hover); transform: translateY(-2px); }
        .fee-card.highlight { border-color: rgba(17,153,250,0.3); }
        .fee-label { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--accent); font-weight: 700; margin-bottom: 8px; }
        .fee-amount { font-size: 2rem; font-weight: 800; color: var(--text-primary); margin-bottom: 8px; }
        .fee-amount small { font-size: 0.5em; color: var(--text-muted); font-weight: 400; }
        .fee-desc { font-size: 0.85rem; color: var(--text-muted); line-height: 1.5; }

        .total-bar {
            background: var(--bg-glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass); border-radius: var(--radius);
            padding: 24px; display: flex; justify-content: space-around; text-align: center; margin: 20px 0;
            background-image: linear-gradient(135deg, rgba(17,153,250,0.08), rgba(139,92,246,0.08));
        }
        .total-item h4 { font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-muted); margin-bottom: 4px; }
        .total-item .amount { font-size: 1.5rem; font-weight: 800; }

        .value-card { background: rgba(17,153,250,0.06); border: 1px solid rgba(17,153,250,0.2); border-radius: var(--radius); padding: 24px; margin: 20px 0; }
        .value-card h3 { color: var(--accent); margin-bottom: 16px; font-weight: 700; }
        .value-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid rgba(17,153,250,0.1); }
        .value-row:last-child { border: none; }
        .value-label { color: var(--accent); font-size: 0.88rem; }
        .value-text { color: var(--text-secondary); font-size: 0.88rem; max-width: 60%; text-align: right; }

        .scenario-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-top: 16px; }
        .scenario { text-align: center; padding: 16px; border-radius: 12px; background: var(--bg-card); border: 1px solid var(--border-glass); }
        .scenario-label { font-size: 0.72rem; text-transform: uppercase; color: var(--text-muted); letter-spacing: 0.05em; }
        .scenario-amount { font-size: 1.3rem; font-weight: 800; color: var(--accent); margin-top: 4px; }

        .roi-card { background: rgba(0,158,126,0.06); border: 1px solid rgba(0,158,126,0.2); border-radius: var(--radius); padding: 24px; margin: 20px 0; }
        .roi-card h3 { color: var(--accent-secondary); margin-bottom: 12px; font-weight: 700; }
        .roi-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin-bottom: 16px; }
        .roi-stat { text-align: center; padding: 16px; background: var(--bg-card); border: 1px solid var(--border-glass); border-radius: 12px; }
        .roi-stat .label { font-size: 0.72rem; text-transform: uppercase; color: var(--accent-secondary); letter-spacing: 0.05em; }
        .roi-stat .val { font-size: 1.5rem; font-weight: 800; color: var(--accent-secondary); margin-top: 4px; }
        .roi-summary { color: var(--text-secondary); font-size: 0.92rem; font-style: italic; padding-top: 12px; border-top: 1px solid rgba(0,158,126,0.15); }

        .section { margin-bottom: 20px; }
        .section h3 { font-size: 1rem; color: var(--text-primary); margin-bottom: 10px; display: flex; align-items: center; gap: 8px; font-weight: 700; }
        .section p { color: var(--text-secondary); font-size: 0.92rem; line-height: 1.7; white-space: pre-wrap; }
        .tag-list { display: flex; flex-wrap: wrap; gap: 8px; }
        .tag { background: rgba(255,255,255,0.06); border: 1px solid var(--border-glass); padding: 6px 14px; border-radius: var(--radius-pill); font-size: 0.82rem; color: var(--text-secondary); }
        .tag.excl { background: rgba(239,68,68,0.08); border-color: rgba(239,68,68,0.2); color: #ef4444; }

        .approve-card {
            background: rgba(0,158,126,0.06); border: 2px solid rgba(0,158,126,0.3);
            border-radius: var(--radius); padding: 32px; text-align: center; margin: 24px 0;
        }
        .approve-card h3 { color: var(--accent-secondary); margin-bottom: 8px; font-weight: 700; }
        .approve-card p { color: var(--text-secondary); margin-bottom: 16px; font-size: 0.92rem; }
        .btn-approve {
            background: var(--accent-secondary); color: #fff; padding: 14px 36px; font-size: 1rem;
            font-weight: 700; border: none; border-radius: var(--radius-pill); cursor: pointer;
            box-shadow: 0 0 24px rgba(0,158,126,0.4); transition: all 0.3s; font-family: 'Inter', sans-serif;
        }
        .btn-approve:hover { transform: translateY(-2px); box-shadow: 0 0 36px rgba(0,158,126,0.5); }

        .approved-banner {
            background: rgba(0,158,126,0.1); border: 1px solid rgba(0,158,126,0.3);
            border-radius: var(--radius); padding: 20px; text-align: center; margin: 20px 0;
        }
        .approved-banner h3 { color: var(--accent-secondary); }

        @media (max-width: 768px) {
            .fee-grid, .scenario-grid, .roi-grid { grid-template-columns: 1fr; }
            .total-bar { flex-direction: column; gap: 16px; }
            .value-row { flex-direction: column; gap: 4px; }
            .value-text { max-width: 100%; text-align: left; }
            .header-container { flex-direction: column; gap: 12px; }
            .nav { flex-wrap: wrap; justify-content: center; }
        }
    </style>
</head>
<body>

<div class="header">
    <div class="header-container">
        <a href="/dashboard" class="logo">◆ Morti</a>
        <nav class="nav">
            <a href="/dashboard">Projects</a>
            <a href="/projects">Projects</a>
            <a href="/profile">Profile</a>
            <a href="/logout">Logout</a>
        </nav>
    </div>
</div>

<div class="main">
    <div class="breadcrumb">
        <a href="/dashboard">Projects</a> → <a href="/projects">Projects</a> → <a href="/projects/<%= encodeId(project.id) %>"><%= project.name %></a> → Proposal
    </div>

    <% if (proposal.approvedAt) { %>
    <div class="approved-banner">
        <h3>✅ Proposal Approved</h3>
        <p style="color: var(--text-secondary); margin-top: 4px; font-size: 0.88rem;">Approved by <%= proposal.approvedBy %> on <%= melbDate(proposal.approvedAt) %></p>
    </div>
    <% } %>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
        <h2 style="font-weight: 800; letter-spacing: -0.03em;">💰 Proposal — <%= project.name %></h2>
        <a href="/projects/<%= encodeId(project.id) %>" class="btn" style="background: rgba(255,255,255,0.06); border: 1px solid var(--border-glass);">← Back to Project</a>
    </div>

    <!-- Header -->
    <div class="card">
        <h3 style="margin-bottom: 4px; font-weight: 700;"><%= proposal.projectName || project.name %></h3>
        <div style="color: var(--text-muted); font-size: 0.88rem;">
            Prepared for: <strong style="color: var(--text-primary);"><%= proposal.clientCompany || 'TBD' %></strong>
            &nbsp;·&nbsp; <%= melbDate(proposal.createdAt) %>
            &nbsp;·&nbsp; All prices ex GST
        </div>

        <% if (proposal.commercialContext) { %>
        <div class="section" style="margin-top: 16px;">
            <h3>🏢 What This Project Delivers</h3>
            <p><%= proposal.commercialContext %></p>
        </div>
        <% } %>
    </div>

    <!-- Labour Analysis -->
    <% if (proposal.labourAnalysis) { %>
    <div class="value-card">
        <h3>📊 Current Cost Analysis</h3>
        <% if (proposal.labourAnalysis.currentProcess) { %>
        <div class="value-row">
            <span class="value-label">Current Process</span>
            <span class="value-text"><%= proposal.labourAnalysis.currentProcess %></span>
        </div>
        <% } %>
        <% if (proposal.labourAnalysis.hoursPerWeek) { %>
        <div class="value-row">
            <span class="value-label">Estimated Effort</span>
            <span class="value-text"><%= proposal.labourAnalysis.hoursPerWeek %> hrs/week</span>
        </div>
        <% } %>
        <div class="value-row" style="border-top: 1px solid rgba(17,153,250,0.2); padding-top: 12px;">
            <span class="value-label" style="font-weight: 700;">Estimated Annual Savings</span>
            <span class="value-text" style="font-size: 1.2rem; font-weight: 800; color: var(--accent);">$<%= (proposal.labourAnalysis.totalAnnualSavings || 0).toLocaleString() %></span>
        </div>
    </div>
    <% } %>

    <!-- Discount Banner -->
    <% if (proposal.discount && (proposal.discount.upfront || proposal.discount.annual || proposal.discount.percentage)) { %>
    <div style="background: rgba(245,158,11,0.08); border: 1px solid rgba(245,158,11,0.25); border-radius: var(--radius); padding: 12px 20px; margin-bottom: 16px; display: flex; align-items: center; gap: 12px;">
        <span style="font-size: 24px;">🏷️</span>
        <div>
            <strong style="color: #f59e0b; font-size: 1rem;"><%= proposal.discount.upfront && proposal.discount.annual ? proposal.discount.upfront + '%/' + proposal.discount.annual + '% Discount (Upfront/Annual)' : (proposal.discount.upfront ? proposal.discount.upfront + '% Upfront Discount' : (proposal.discount.annual ? proposal.discount.annual + '% Annual Discount' : proposal.discount.percentage + '% Discount')) %></strong>
            <% if (proposal.discount.reason) { %><div style="color: var(--text-muted); font-size: 0.82rem;"><%= proposal.discount.reason %></div><% } %>
        </div>
    </div>
    <% } %>

    <!-- Two Fee Cards -->
    <div class="fee-grid">
        <div class="fee-card highlight">
            <div class="fee-label">1. Upfront Fee</div>
            <% if (proposal.upfrontFee && proposal.upfrontFee.originalTotal && proposal.upfrontFee.originalTotal !== proposal.upfrontFee.total) { %>
                <div class="fee-amount"><span style="text-decoration: line-through; color: var(--text-muted); font-size: 0.7em;">$<%= proposal.upfrontFee.originalTotal.toLocaleString() %></span> $<%= (proposal.upfrontFee.total || 0).toLocaleString() %></div>
            <% } else { %>
                <div class="fee-amount">$<%= ((proposal.upfrontFee && proposal.upfrontFee.total) || proposal.totalUpfront || 0).toLocaleString() %></div>
            <% } %>
            <div class="fee-desc">Discovery, design, development, testing, and deployment — everything to get you live.</div>
        </div>
        <div class="fee-card" style="border-color: rgba(0,158,126,0.3);">
            <div class="fee-label" style="color: var(--accent-secondary);">2. Annual Fee</div>
            <% if (proposal.annualFee && proposal.annualFee.originalTotal && proposal.annualFee.originalTotal !== proposal.annualFee.total) { %>
                <div class="fee-amount"><span style="text-decoration: line-through; color: var(--text-muted); font-size: 0.7em;">$<%= proposal.annualFee.originalTotal.toLocaleString() %></span> $<%= (proposal.annualFee.total || 0).toLocaleString() %> <small>/yr</small></div>
            <% } else { %>
                <div class="fee-amount">$<%= ((proposal.annualFee && proposal.annualFee.total) || proposal.annualMaintenance || 0).toLocaleString() %> <small>/yr</small></div>
            <% } %>
            <% if (proposal.annualFee) { %>
            <div style="font-size: 0.88rem; color: var(--text-secondary); background: rgba(0,158,126,0.08); padding: 8px 12px; border-radius: 8px; margin: 8px 0;">
                $<%= (proposal.annualFee.monthlyEquivalent || 0).toLocaleString() %>/month
            </div>
            <div class="fee-desc"><%= proposal.annualFee.includes %></div>
            <% } %>
        </div>
    </div>

    <!-- Summary Bar -->
    <div class="total-bar">
        <div class="total-item">
            <h4>Upfront (one-off)</h4>
            <div class="amount">$<%= ((proposal.upfrontFee && proposal.upfrontFee.total) || proposal.totalUpfront || 0).toLocaleString() %></div>
        </div>
        <div class="total-item">
            <h4>Annual Fee</h4>
            <div class="amount">$<%= ((proposal.annualFee && proposal.annualFee.total) || proposal.annualMaintenance || 0).toLocaleString() %> <small>/yr</small></div>
        </div>
        <div class="total-item">
            <h4>Timeline</h4>
            <div class="amount" style="font-size: 1.2rem;"><%= proposal.timeline %></div>
        </div>
    </div>

    <!-- ROI -->
    <% const roi = proposal.roiIllustration || proposal.roi; %>
    <% if (roi) { %>
    <div class="roi-card">
        <h3>📈 Your Return on Investment</h3>
        <div class="roi-grid">
            <% if (roi.netAnnualBenefit) { %>
            <div class="roi-stat">
                <div class="label">Net Annual Benefit</div>
                <div class="val">$<%= (roi.netAnnualBenefit || 0).toLocaleString() %></div>
            </div>
            <% } %>
            <% if (roi.roiMultiple) { %>
            <div class="roi-stat">
                <div class="label">ROI Multiple</div>
                <div class="val"><%= roi.roiMultiple %></div>
            </div>
            <% } %>
            <% if (roi.paybackOnUpfront || roi.paybackPeriod) { %>
            <div class="roi-stat">
                <div class="label">Upfront Payback</div>
                <div class="val"><%= roi.paybackOnUpfront || roi.paybackPeriod %></div>
            </div>
            <% } %>
        </div>
        <% if (roi.summary) { %>
        <div class="roi-summary"><%= roi.summary %></div>
        <% } %>
    </div>
    <% } %>

    <!-- Assumptions & Exclusions -->
    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
        <div class="card">
            <h3 style="margin-bottom: 12px; font-weight: 700;">📌 Assumptions</h3>
            <div class="tag-list">
                <% (proposal.assumptions || []).forEach(a => { %>
                    <span class="tag"><%= a %></span>
                <% }) %>
            </div>
        </div>
        <div class="card">
            <h3 style="margin-bottom: 12px; font-weight: 700;">🚫 Exclusions</h3>
            <div class="tag-list">
                <% (proposal.exclusions || []).forEach(e => { %>
                    <span class="tag excl"><%= e %></span>
                <% }) %>
            </div>
        </div>
    </div>

    <!-- Approve -->
    <% if (!proposal.approvedAt) { %>
    <div class="approve-card">
        <h3>Ready to proceed?</h3>
        <p>By approving this proposal, you confirm acceptance of the scope, fees, and timeline outlined above.</p>
        <form method="POST" action="/customer/projects/<%= encodeId(project.id) %>/proposal/approve" onsubmit="return confirm('Approve this proposal? This confirms your acceptance of the scope and fees.')">
            <button type="submit" class="btn-approve">✅ Approve Proposal</button>
        </form>
    </div>
    <% } %>

    <div class="card" style="text-align: center; color: var(--text-muted); font-size: 0.85rem;">
        Valid until: <%= proposal.validUntil || '30 days from generation' %>
        <div style="margin-top: 8px; font-weight: 600; color: var(--text-secondary);">All prices are in AUD and exclude GST.</div>
    </div>
</div>

</body>
</html>
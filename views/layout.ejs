<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Morti Projects - <%= title || 'Voice-Powered Requirements Gathering' %></title>
    <link href="/style.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #0a1628;
            --bg-primary: #0f1d32;
            --bg-card: #142640;
            --bg-glass: rgba(15, 29, 50, 0.6);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa;
            --accent-glow: rgba(17, 153, 250, 0.4);
            --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #009e7e;
            --accent-purple: #8b5cf6;
            --text-primary: #f0f4f8;
            --text-secondary: rgba(240, 244, 248, 0.7);
            --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px;
            --radius-pill: 50px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--bg-deep); color: var(--text-primary); line-height: 1.6; -webkit-font-smoothing: antialiased; }
        a { text-decoration: none; color: inherit; }

        /* Header */
        .header {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-glass);
            color: var(--text-primary);
            padding: 16px 0;
        }
        .header-container {
            max-width: 1200px; margin: 0 auto; display: flex; align-items: center;
            justify-content: space-between; padding: 0 20px;
        }
        .logo { font-size: 20px; font-weight: 800; display: flex; align-items: center; gap: 8px; text-decoration: none; color: var(--text-primary); }
        .nav { display: flex; gap: 8px; align-items: center; }
        .nav a {
            color: var(--text-secondary); text-decoration: none; padding: 8px 14px;
            border-radius: var(--radius-pill); transition: all 0.2s; font-weight: 500; font-size: 0.88rem;
        }
        .nav a:hover { color: var(--text-primary); background: rgba(255,255,255,0.05); }
        .nav a.active { color: var(--accent); background: var(--accent-dim); }
        .user-menu { position: relative; display: inline-block; }
        .user-name {
            background: rgba(255,255,255,0.06); padding: 8px 14px; border-radius: var(--radius-pill);
            cursor: pointer; font-weight: 500; display: flex; align-items: center; gap: 6px;
            font-size: 0.88rem; color: var(--text-secondary); border: 1px solid var(--border-glass);
            transition: all 0.2s;
        }
        .user-name:hover { border-color: var(--border-glass-hover); color: var(--text-primary); }
        .user-dropdown {
            position: absolute; right: 0; top: 100%;
            background: var(--bg-card); border: 1px solid var(--border-glass);
            min-width: 200px; border-radius: 12px; display: none; z-index: 100; margin-top: 8px;
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.4);
        }
        .user-dropdown a {
            display: block; padding: 12px 16px; color: var(--text-secondary);
            text-decoration: none; border-bottom: 1px solid var(--border-glass);
            font-size: 0.85rem; transition: all 0.2s;
        }
        .user-dropdown a:hover { background: rgba(255,255,255,0.03); color: var(--text-primary); }
        .user-dropdown a:last-child { border-bottom: none; }
        .user-menu:hover .user-dropdown { display: block; }

        /* Breadcrumbs */
        .breadcrumbs { background: var(--bg-primary); padding: 12px 20px; border-bottom: 1px solid var(--border-glass); }
        .breadcrumb-container { max-width: 1200px; margin: 0 auto; font-size: 14px; color: var(--text-muted); }
        .breadcrumb-container a { color: var(--accent); text-decoration: none; }
        .breadcrumb-container a:hover { text-decoration: underline; }

        /* Main content */
        .main { max-width: 1200px; margin: 0 auto; padding: 24px; min-height: calc(100vh - 200px); }

        /* Cards */
        .card {
            background: var(--bg-glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass); border-radius: var(--radius);
            padding: 24px; margin-bottom: 20px; transition: border-color 0.3s;
        }
        .card:hover { border-color: var(--border-glass-hover); }
        .card h2 { margin-bottom: 16px; color: var(--text-primary); font-size: 1.3rem; font-weight: 700; }
        .card h3 { margin-bottom: 12px; color: var(--text-secondary); font-size: 1.1rem; }

        /* Buttons */
        .btn {
            display: inline-flex; align-items: center; justify-content: center;
            padding: 10px 24px; border-radius: var(--radius-pill); font-size: 0.88rem;
            font-weight: 600; cursor: pointer; transition: all 0.3s; border: none;
            color: #fff; letter-spacing: -0.01em; text-decoration: none;
        }
        .btn-primary, .btn { background: var(--accent); box-shadow: 0 0 20px var(--accent-glow); }
        .btn-primary:hover, .btn:hover { background: #2aa8ff; transform: translateY(-2px); box-shadow: 0 0 32px var(--accent-glow); }
        .btn-sm { padding: 6px 14px; font-size: 0.78rem; }
        .btn-secondary { background: rgba(255,255,255,0.06); border: 1px solid var(--border-glass); box-shadow: none; }
        .btn-secondary:hover { background: rgba(255,255,255,0.1); border-color: var(--border-glass-hover); box-shadow: none; }
        .btn-danger { background: #ef4444; box-shadow: 0 0 16px rgba(239,68,68,0.3); }
        .btn-danger:hover { background: #f87171; box-shadow: 0 0 24px rgba(239,68,68,0.4); }
        .btn-success { background: var(--accent-secondary); box-shadow: 0 0 16px rgba(0, 158, 126, 0.3); }
        .btn-success:hover { background: #00e6b8; box-shadow: 0 0 24px rgba(0, 158, 126, 0.4); }

        /* Forms */
        .form-group { margin-bottom: 16px; }
        .form-group label { display: block; margin-bottom: 6px; font-weight: 600; color: var(--text-secondary); font-size: 0.85rem; }
        .form-control {
            width: 100%; padding: 10px 12px; border: 1px solid var(--border-glass);
            border-radius: 10px; font-size: 14px; transition: all 0.2s;
            background: var(--bg-card); color: var(--text-primary);
        }
        .form-control:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-dim); }

        /* Tables */
        .table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        .table th, .table td { text-align: left; padding: 12px; border-bottom: 1px solid var(--border-glass); }
        .table th { background: rgba(15, 29, 50, 0.4); font-weight: 600; color: var(--text-muted); font-size: 0.78rem; text-transform: uppercase; letter-spacing: 0.06em; }
        .table tbody tr:hover { background: rgba(17, 153, 250, 0.05); }

        /* Stats grid */
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 16px; margin-bottom: 28px; }
        .stat-card {
            background: var(--bg-glass); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass); border-radius: var(--radius);
            padding: 24px; text-align: center; transition: all 0.3s;
        }
        .stat-card:hover { border-color: var(--border-glass-hover); transform: translateY(-2px); }
        .stat-number { font-size: 2rem; font-weight: 800; color: var(--accent); margin-bottom: 4px; }
        .stat-label { font-size: 0.78rem; color: var(--text-muted); font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; }

        /* Alerts */
        .alert { padding: 12px 16px; border-radius: 12px; margin-bottom: 20px; border: 1px solid; }
        .alert-success { background: rgba(0, 158, 126, 0.1); color: var(--accent-secondary); border-color: rgba(0, 158, 126, 0.2); }
        .alert-error { background: rgba(239, 68, 68, 0.1); color: #f87171; border-color: rgba(239, 68, 68, 0.2); }
        .alert-info { background: var(--accent-dim); color: var(--accent); border-color: rgba(17, 153, 250, 0.2); }

        /* Empty state */
        .empty-state { text-align: center; padding: 60px 20px; color: var(--text-muted); }
        .empty-state .emoji { font-size: 48px; margin-bottom: 16px; display: block; }
        .empty-state h3 { margin-bottom: 8px; color: var(--text-secondary); }

        @media (max-width: 768px) {
            .header-container { flex-direction: column; gap: 12px; text-align: center; }
            .nav { flex-wrap: wrap; justify-content: center; }
            .main { padding: 16px; }
            .card { padding: 16px; }
            .stats-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="header-container">
            <a href="<%= user.role === 'admin' ? '/admin' : '/dashboard' %>" class="logo">
                🎙️ Morti Projects
            </a>

            <nav class="nav">
                <% if (user.role === 'admin') { %>
                    <a href="/admin" class="<%= currentPage === 'admin-dashboard' ? 'active' : '' %>">Dashboard</a>
                    <a href="/admin/customers" class="<%= currentPage === 'admin-customers' ? 'active' : '' %>">Customers</a>
                    <a href="/admin/projects" class="<%= currentPage === 'admin-projects' ? 'active' : '' %>">All Projects</a>
                <% } else { %>
                    <a href="/dashboard" class="<%= currentPage === 'customer-dashboard' ? 'active' : '' %>">Dashboard</a>
                    <a href="/projects" class="<%= currentPage === 'customer-projects' ? 'active' : '' %>">Projects</a>
                <% } %>

                <div class="user-menu">
                    <div class="user-name">
                        👤 <%= user.name %> ▼
                    </div>
                    <div class="user-dropdown">
                        <a href="#">📧 <%= user.email %></a>
                        <a href="#">🏢 <%= user.company || 'N/A' %></a>
                        <a href="#">👑 <%= user.role.charAt(0).toUpperCase() + user.role.slice(1) %></a>
                        <a href="/logout">🚪 Logout</a>
                    </div>
                </div>
            </nav>
        </div>
    </div>

    <% if (breadcrumbs && breadcrumbs.length > 0) { %>
    <div class="breadcrumbs">
        <div class="breadcrumb-container">
            <% breadcrumbs.forEach((crumb, index) => { %>
                <% if (index > 0) { %> / <% } %>
                <% if (crumb.url && index < breadcrumbs.length - 1) { %>
                    <a href="<%= crumb.url %>"><%= crumb.name %></a>
                <% } else { %>
                    <%= crumb.name %>
                <% } %>
            <% }) %>
        </div>
    </div>
    <% } %>

    <main class="main">
        <% if (message) { %>
            <div class="alert alert-<%= messageType || 'info' %>">
                <%= message %>
            </div>
        <% } %>

        <!-- Page content will be rendered here by the individual templates -->
    </main>

    <script>
        // Auto-hide alerts after 5 seconds
        setTimeout(() => {
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(alert => {
                alert.style.opacity = '0';
                setTimeout(() => alert.remove(), 300);
            });
        }, 5000);
    </script>
</body>
</html>
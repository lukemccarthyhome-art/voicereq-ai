<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Morti — Talk. Automate. Done. Forever.</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <meta name="description" content="Describe your business process to Morti. We deploy an AI agent that automates the workflow — from invoice approvals to onboarding. Built for Australian SMBs. No technical knowledge needed.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #050d1a;
            --bg-primary: #0a1628;
            --bg-card: #0f1d32;
            --bg-card-hover: #142640;
            --bg-glass: rgba(15, 29, 50, 0.6);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa;
            --accent-glow: rgba(17, 153, 250, 0.4);
            --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #009e7e;
            --accent-secondary-glow: rgba(0, 158, 126, 0.3);
            --accent-purple: #8b5cf6;
            --accent-purple-glow: rgba(139, 92, 246, 0.3);
            --text-primary: #f0f4f8;
            --text-secondary: rgba(240, 244, 248, 0.7);
            --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px;
            --radius-sm: 10px;
            --radius-pill: 50px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: var(--text-primary); background: var(--bg-deep);
            overflow-x: hidden; font-weight: 400;
            -webkit-font-smoothing: antialiased;
        }
        a { text-decoration: none; color: inherit; }
        html { scroll-behavior: smooth; }

        /* ========== GLASS UTILITY ========== */
        .glass {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            border-radius: var(--radius);
        }
        .glass:hover { border-color: var(--border-glass-hover); }

        /* ========== BUTTONS ========== */
        .btn {
            display: inline-flex; align-items: center; justify-content: center;
            padding: 14px 36px; border-radius: var(--radius-pill); font-size: 0.95rem;
            font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: none; letter-spacing: -0.01em; position: relative;
        }
        .btn-primary {
            background: var(--accent); color: #fff;
            box-shadow: 0 0 20px var(--accent-glow), 0 4px 12px rgba(0, 0, 0, 0.3);
        }
        .btn-primary:hover {
            background: #2aa8ff; transform: translateY(-2px);
            box-shadow: 0 0 32px var(--accent-glow), 0 8px 24px rgba(0, 0, 0, 0.4);
        }
        .btn-glass {
            background: rgba(255, 255, 255, 0.06); color: #fff;
            border: 1px solid rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
        }
        .btn-glass:hover {
            background: rgba(255, 255, 255, 0.12); border-color: rgba(255, 255, 255, 0.25);
            transform: translateY(-2px);
        }
        .btn-white {
            background: #fff; color: var(--bg-deep); font-weight: 700;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.15), 0 4px 12px rgba(0, 0, 0, 0.3);
        }
        .btn-white:hover {
            transform: translateY(-2px);
            box-shadow: 0 0 32px rgba(255, 255, 255, 0.2), 0 8px 24px rgba(0, 0, 0, 0.4);
        }
        .btn-lg { padding: 18px 44px; font-size: 1.05rem; }

        /* ========== CONTAINER ========== */
        .container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }

        /* ========== FADE IN ========== */
        .fade-in {
            opacity: 0; transform: translateY(32px);
            transition: opacity 0.8s ease-out, transform 0.8s ease-out;
        }
        .fade-in.visible { opacity: 1; transform: translateY(0); }
        .fade-in-delay-1 { transition-delay: 0.15s; }
        .fade-in-delay-2 { transition-delay: 0.3s; }
        .fade-in-delay-3 { transition-delay: 0.45s; }

        /* ========== NAV ========== */
        nav {
            position: fixed; top: 0; width: 100%; z-index: 100;
            display: flex; align-items: center; justify-content: space-between;
            padding: 16px 40px;
            background: rgba(5, 13, 26, 0.8);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-glass);
            transition: all 0.3s;
        }
        nav.scrolled { background: rgba(5, 13, 26, 0.95); }
        .nav-brand { font-size: 1.3rem; font-weight: 800; color: #fff; letter-spacing: -0.02em; }
        .nav-links { display: flex; gap: 32px; align-items: center; }
        .nav-links a.nav-text {
            font-size: 0.88rem; color: var(--text-muted); transition: color 0.2s;
            font-weight: 500; letter-spacing: 0.01em;
        }
        .nav-links a.nav-text:hover { color: #fff; }
        .nav-links a.nav-text.active { color: var(--accent); }
        .nav-cta {
            background: var(--accent); color: #fff; padding: 10px 28px;
            border-radius: var(--radius-pill); font-size: 0.88rem; font-weight: 600;
            box-shadow: 0 0 16px var(--accent-glow);
            transition: all 0.3s;
        }
        .nav-cta:hover { background: #2aa8ff; box-shadow: 0 0 24px var(--accent-glow); transform: translateY(-1px); }
        /* Nav dropdown */
        .nav-dropdown { position: relative; }
        .nav-dropdown-trigger { cursor: pointer; }
        .nav-arrow { font-size: 0.7em; margin-left: 2px; transition: transform 0.2s; display: inline-block; }
        .nav-dropdown:hover .nav-arrow { transform: rotate(180deg); }
        .nav-dropdown-menu {
            position: absolute; top: calc(100% + 12px); left: 50%; transform: translateX(-50%);
            min-width: 220px; padding: 8px;
            background: rgba(10, 22, 40, 0.95);
            backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-glass); border-radius: 12px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.4);
            opacity: 0; visibility: hidden; transition: opacity 0.2s, visibility 0.2s;
            z-index: 200;
        }
        .nav-dropdown:hover .nav-dropdown-menu { opacity: 1; visibility: visible; }
        .nav-dropdown-item {
            display: block; padding: 10px 16px; font-size: 0.88rem; font-weight: 500;
            color: var(--text-muted); border-radius: 8px; transition: all 0.2s;
        }
        .nav-dropdown-item:hover { color: #fff; background: rgba(255,255,255,0.06); }

        .hamburger { display: none; background: none; border: none; font-size: 1.5rem; cursor: pointer; padding: 4px 8px; color: #fff; }
        @media (max-width: 768px) {
            nav { padding: 12px 20px; }
            .hamburger { display: block; }
            .nav-links {
                display: none; flex-direction: column; position: absolute; top: 100%;
                left: 0; right: 0; background: rgba(10, 22, 40, 0.98);
                backdrop-filter: blur(20px); padding: 16px 20px; gap: 4px;
                border-bottom: 1px solid var(--border-glass);
            }
            .nav-links.open { display: flex; }
            .nav-links a.nav-text { padding: 12px 0; font-size: 1rem; }
            .nav-cta { text-align: center; margin-top: 8px; display: block; }
            .nav-dropdown-menu {
                position: static; transform: none; opacity: 1; visibility: visible;
                background: transparent; border: none; box-shadow: none;
                backdrop-filter: none; padding: 0 0 0 16px;
                display: none;
            }
            .nav-dropdown.open .nav-dropdown-menu { display: block; }
            .nav-dropdown-trigger { padding: 12px 0; font-size: 1rem; }
        }

        /* ========== HERO ========== */
        .hero {
            min-height: 100vh; display: flex; align-items: center; justify-content: center;
            background: var(--bg-deep);
            padding: 120px 24px 40px; text-align: left; color: #fff;
            position: relative; overflow: hidden;
        }
        .hero-orb {
            position: absolute; border-radius: 50%; pointer-events: none;
            filter: blur(100px); animation: orbFloat 16s ease-in-out infinite;
        }
        .hero-orb-1 { width: 600px; height: 600px; background: rgba(17, 153, 250, 0.12); top: -10%; left: -10%; }
        .hero-orb-2 { width: 500px; height: 500px; background: rgba(139, 92, 246, 0.1); bottom: -5%; right: -10%; animation-delay: -5s; }
        .hero-orb-3 { width: 350px; height: 350px; background: rgba(0, 158, 126, 0.08); top: 40%; left: 55%; animation-delay: -10s; }
        @keyframes orbFloat {
            0%, 100% { transform: translate(0, 0) scale(1); }
            33% { transform: translate(40px, -30px) scale(1.08); }
            66% { transform: translate(-30px, 20px) scale(0.92); }
        }
        .hero-grid {
            position: absolute; inset: 0; opacity: 0.03;
            background-image:
                linear-gradient(rgba(255,255,255,0.5) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255,255,255,0.5) 1px, transparent 1px);
            background-size: 80px 80px;
        }
        .hero-inner {
            position: relative; z-index: 1; max-width: 1200px; width: 100%;
            display: grid; grid-template-columns: 1fr 1.2fr; gap: 40px; align-items: center;
        }
        .hero-text { display: flex; flex-direction: column; }
        .hero h1 {
            font-size: clamp(3rem, 6.5vw, 5.2rem); font-weight: 900; margin-bottom: 28px;
            line-height: 1.02; letter-spacing: -0.05em;
            animation: fadeUp 0.8s ease-out;
        }
        .hero h1 .gradient-text {
            background: linear-gradient(135deg, var(--accent) 0%, var(--accent-secondary) 50%, var(--accent-purple) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
        }
        .hero .hero-sub {
            font-size: clamp(1.05rem, 2vw, 1.25rem); max-width: 640px; margin: 0 0 20px;
            color: var(--text-secondary); line-height: 1.8; font-weight: 300;
            animation: fadeUp 0.8s ease-out 0.15s both;
        }
        .hero .hero-explainer {
            font-size: 0.92rem; max-width: 580px; margin: 0 0 32px;
            color: var(--text-muted); line-height: 1.7; font-weight: 400;
            animation: fadeUp 0.8s ease-out 0.2s both;
            padding: 16px 24px; border-radius: var(--radius-sm);
            background: rgba(17, 153, 250, 0.06);
            border: 1px solid rgba(17, 153, 250, 0.1);
        }
        /* Hero image (crypto.com bleed style) */
        .hero-image-wrap {
            width: 100%;
            animation: fadeUp 1s ease-out 0.25s both;
            position: relative;
            perspective: 1200px;
        }
        .hero-image-inner {
            transform: rotateY(-6deg) rotateX(4deg);
            transform-origin: center center;
            border-radius: 16px;
            overflow: hidden;
            position: relative;
        }
        /* Multi-edge gradient mask — dissolves into background on all sides */
        .hero-image-inner::after {
            content: ''; position: absolute; inset: 0; pointer-events: none; z-index: 2;
            border-radius: 16px;
            background:
                linear-gradient(to bottom, transparent 30%, var(--bg-deep) 98%),
                linear-gradient(to right, var(--bg-deep) 0%, transparent 12%, transparent 88%, var(--bg-deep) 100%);
        }
        /* Fake dashboard app styles */
        .hero-app {
            background: #0b1221; border-radius: 16px; overflow: hidden;
            font-family: 'Inter', sans-serif; color: #f0f4f8;
            font-size: 13px; text-align: left;
        }
        .hero-app-nav {
            display: flex; align-items: center; justify-content: space-between;
            padding: 12px 24px; border-bottom: 1px solid rgba(255,255,255,0.06);
            background: rgba(255,255,255,0.02);
        }
        .hero-app-logo { display: flex; align-items: center; }
        .hero-app-nav-links { display: flex; gap: 24px; }
        .hero-app-nav-links span { color: rgba(240,244,248,0.4); font-size: 13px; font-weight: 500; }
        .hero-app-nav-links span.active { color: #f0f4f8; }
        .hero-app-nav-right { display: flex; align-items: center; gap: 12px; }
        .hero-app-pill {
            background: rgba(17,153,250,0.15); color: #1199fa; font-size: 11px; font-weight: 600;
            padding: 4px 12px; border-radius: 20px;
        }
        .hero-app-avatar {
            width: 32px; height: 32px; border-radius: 50%; background: linear-gradient(135deg, #1199fa, #8b5cf6);
            display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700;
        }
        .hero-app-body { display: flex; min-height: 340px; }
        .hero-app-sidebar {
            width: 240px; padding: 20px; border-right: 1px solid rgba(255,255,255,0.06);
            display: flex; flex-direction: column; gap: 16px;
        }
        .hero-app-stat {
            padding: 14px; border-radius: 10px; background: rgba(255,255,255,0.03);
            border: 1px solid rgba(255,255,255,0.05);
        }
        .hero-app-stat-label { font-size: 11px; color: rgba(240,244,248,0.4); margin-bottom: 6px; text-transform: uppercase; letter-spacing: 0.5px; }
        .hero-app-stat-value { font-size: 28px; font-weight: 800; color: #f0f4f8; line-height: 1; }
        .hero-app-stat-change { font-size: 11px; margin-top: 4px; }
        .hero-app-stat-change.positive { color: #00c896; }
        .hero-app-recent { margin-top: auto; }
        .hero-app-recent-title { font-size: 11px; color: rgba(240,244,248,0.4); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 10px; }
        .hero-app-recent-item { font-size: 12px; color: rgba(240,244,248,0.6); padding: 6px 0; display: flex; align-items: center; gap: 8px; }
        .hero-app-recent-item em { color: rgba(240,244,248,0.3); font-style: normal; }
        .dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
        .dot.green { background: #00c896; }
        .dot.blue { background: #1199fa; }
        .dot.purple { background: #8b5cf6; }
        .hero-app-main { flex: 1; padding: 20px; display: flex; flex-direction: column; gap: 16px; }
        .hero-app-chart-header { display: flex; justify-content: space-between; align-items: center; }
        .hero-app-chart-tabs { display: flex; gap: 4px; }
        .hero-app-chart-tabs span {
            padding: 4px 12px; border-radius: 20px; font-size: 11px; font-weight: 600;
            color: rgba(240,244,248,0.4); cursor: default;
        }
        .hero-app-chart-tabs span.active { background: rgba(17,153,250,0.15); color: #1199fa; }
        .hero-app-chart { height: 180px; border-radius: 10px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.05); overflow: hidden; }
        .hero-app-projects { display: flex; flex-direction: column; gap: 2px; }
        .hero-app-project-row {
            display: flex; align-items: center; gap: 12px; padding: 10px 14px;
            border-radius: 8px; background: rgba(255,255,255,0.02);
        }
        .hero-app-project-name { flex: 1; font-weight: 500; color: #f0f4f8; }
        .hero-app-project-status {
            font-size: 11px; padding: 3px 10px; border-radius: 20px; font-weight: 600;
        }
        .hero-app-project-status.running { background: rgba(0,200,150,0.12); color: #00c896; }
        .hero-app-project-metric { font-size: 12px; color: rgba(240,244,248,0.4); min-width: 100px; text-align: right; }
        @media (max-width: 768px) {
            .hero-app-body { flex-direction: column; }
            .hero-app-sidebar { width: 100%; border-right: none; border-bottom: 1px solid rgba(255,255,255,0.06); flex-direction: row; flex-wrap: wrap; }
            .hero-app-stat { flex: 1; min-width: 120px; }
            .hero-app-recent { display: none; }
            .hero-app-nav-links { display: none; }
            .hero-app-project-metric { display: none; }
        }

        /* Video section (moved lower) */
        .video-section {
            padding: 100px 24px; position: relative;
        }
        .video-section::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .video-wrap {
            max-width: 820px; margin: 0 auto;
        }
        .video-container {
            position: relative; border-radius: 20px; overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.1);
            background: var(--bg-glass);
            box-shadow:
                0 0 40px rgba(17, 153, 250, 0.1),
                0 20px 60px rgba(0, 0, 0, 0.5),
                inset 0 1px 0 rgba(255, 255, 255, 0.06);
        }
        .video-container video {
            width: 100%; display: block; border-radius: 19px;
        }
        .video-play-overlay {
            position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;
            cursor: pointer; z-index: 2; transition: opacity 0.4s;
        }
        .video-play-overlay.hidden { opacity: 0; pointer-events: none; }
        .video-play-btn {
            width: 72px; height: 72px; border-radius: 50%;
            background: rgba(255, 255, 255, 0.12);
            backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            display: flex; align-items: center; justify-content: center;
            transition: all 0.3s;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.3);
        }
        .video-play-btn::after {
            content: ''; display: block;
            width: 0; height: 0;
            border-style: solid;
            border-width: 12px 0 12px 22px;
            border-color: transparent transparent transparent rgba(255, 255, 255, 0.9);
            margin-left: 4px;
        }
        .video-play-overlay:hover .video-play-btn {
            background: rgba(17, 153, 250, 0.25);
            border-color: rgba(17, 153, 250, 0.4);
            transform: scale(1.08);
            box-shadow: 0 0 40px var(--accent-glow);
        }
        @media (max-width: 768px) {
            .video-play-btn { width: 56px; height: 56px; }
            .video-play-btn::after { border-width: 9px 0 9px 16px; margin-left: 3px; }
        }
        .hero-buttons {
            display: flex; gap: 16px; justify-content: flex-start; flex-wrap: wrap;
            animation: fadeUp 0.8s ease-out 0.35s both;
        }
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(32px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ========== WHO IS THIS FOR ========== */
        .who-section {
            padding: 100px 24px 0; position: relative;
        }
        .who-section::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .who-grid {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px;
            max-width: 1000px; margin: 0 auto;
        }
        .who-card {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-radius: var(--radius); padding: 40px 32px;
            border: 1px solid var(--border-glass);
            transition: all 0.3s;
        }
        .who-card:hover {
            border-color: var(--border-glass-hover);
            transform: translateY(-4px);
            box-shadow: 0 16px 48px rgba(0, 0, 0, 0.25);
        }
        .who-card .who-icon {
            font-size: 2.2rem; margin-bottom: 20px;
        }
        .who-card h3 {
            font-size: 1.25rem; font-weight: 700; margin-bottom: 14px; color: var(--text-primary);
        }
        .who-card p {
            font-size: 1.05rem; color: var(--text-secondary); line-height: 1.75; font-weight: 300;
        }

        /* ========== EXAMPLES STRIP ========== */
        .examples-section {
            padding: 100px 24px; position: relative;
        }
        .examples-section::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .examples-grid {
            display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px;
            max-width: 900px; margin: 0 auto;
        }
        .example-item {
            display: flex; align-items: flex-start; gap: 18px;
            padding: 28px; border-radius: var(--radius-sm);
            background: rgba(17, 153, 250, 0.04);
            border: 1px solid rgba(17, 153, 250, 0.08);
            transition: all 0.3s;
        }
        .example-item:hover {
            background: rgba(17, 153, 250, 0.07);
            border-color: rgba(17, 153, 250, 0.15);
        }
        .example-item .example-icon {
            font-size: 1.4rem; flex-shrink: 0; margin-top: 2px;
        }
        .example-item .example-text strong {
            display: block; font-size: 1.12rem; font-weight: 600; color: var(--text-primary); margin-bottom: 8px;
        }
        .example-item .example-text span {
            font-size: 1rem; color: var(--text-secondary); line-height: 1.75;
        }

        /* ========== JOURNEY / HOW IT WORKS ========== */
        .journey { padding: 0; }
        .journey-step {
            padding: 120px 24px; position: relative; overflow: hidden;
        }
        .journey-step::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .journey-inner {
            max-width: 1100px; margin: 0 auto;
            display: grid; grid-template-columns: 1fr 1fr; gap: 80px; align-items: center;
        }
        .journey-step:nth-child(even) .journey-inner { direction: rtl; }
        .journey-step:nth-child(even) .journey-inner > * { direction: ltr; }
        .journey-num {
            display: inline-flex; align-items: center; justify-content: center;
            width: 48px; height: 48px; border-radius: 14px; font-size: 1.1rem; font-weight: 800;
            background: var(--accent-dim); color: var(--accent);
            border: 1px solid rgba(17, 153, 250, 0.2);
            margin-bottom: 20px;
        }
        .journey-label {
            font-size: 0.82rem; text-transform: uppercase; letter-spacing: 0.16em;
            color: var(--accent); font-weight: 600; margin-bottom: 14px;
        }
        .journey-content h2 {
            font-size: clamp(1.8rem, 3vw, 2.5rem); font-weight: 800; color: var(--text-primary);
            letter-spacing: -0.03em; line-height: 1.12; margin-bottom: 18px;
        }
        .journey-content p {
            font-size: 1.05rem; color: var(--text-secondary); line-height: 1.8; font-weight: 300;
            max-width: 480px;
        }
        .journey-content .journey-example {
            margin-top: 14px; font-size: 0.88rem; color: var(--text-muted); font-style: italic;
            padding-left: 16px; border-left: 2px solid rgba(17, 153, 250, 0.2);
        }
        .journey-visual {
            display: flex; align-items: center; justify-content: center;
        }
        .journey-icon-box {
            width: 100%; aspect-ratio: 4/3; border-radius: 20px; position: relative;
            display: flex; align-items: center; justify-content: center;
            overflow: hidden;
            background: var(--bg-glass);
            backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--border-glass);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            transition: all 0.4s;
        }
        .journey-icon-box:hover {
            border-color: var(--border-glass-hover);
            box-shadow: 0 12px 48px rgba(0, 0, 0, 0.3);
            transform: translateY(-4px);
        }
        .journey-icon-box img {
            width: 70%; height: 70%; object-fit: contain;
            filter: drop-shadow(0 8px 24px rgba(17, 153, 250, 0.15));
            transition: transform 0.4s;
        }
        .journey-icon-box:hover img { transform: scale(1.06) translateY(-4px); }
        .journey-step:nth-child(1) .journey-icon-box { box-shadow: 0 8px 40px rgba(17, 153, 250, 0.08); }
        .journey-step:nth-child(2) .journey-icon-box { box-shadow: 0 8px 40px rgba(139, 92, 246, 0.08); }
        .journey-step:nth-child(3) .journey-icon-box { box-shadow: 0 8px 40px rgba(0, 158, 126, 0.08); }
        .journey-step:nth-child(4) .journey-icon-box { box-shadow: 0 8px 40px rgba(17, 153, 250, 0.08); }
        .journey-step:nth-child(5) .journey-icon-box { box-shadow: 0 8px 40px rgba(139, 92, 246, 0.08); }
        .journey-step:nth-child(6) .journey-icon-box { box-shadow: 0 8px 40px rgba(0, 158, 126, 0.08); }

        /* ========== SOCIAL PROOF ========== */
        .social-proof {
            padding: 120px 24px; position: relative; overflow: hidden;
        }
        .social-proof::before {
            content: ''; position: absolute; inset: 0;
            background: radial-gradient(ellipse at 50% 0%, rgba(17, 153, 250, 0.06), transparent 60%);
        }
        .social-proof .container { position: relative; z-index: 1; }
        .social-proof .section-label { color: var(--accent); }
        .social-proof h2 {
            text-align: center; font-size: clamp(1.8rem, 3vw, 2.5rem); font-weight: 800;
            margin-bottom: 16px; letter-spacing: -0.03em;
        }
        .social-proof .section-sub {
            text-align: center; color: var(--text-muted); max-width: 500px;
            margin: 0 auto 64px; line-height: 1.8; font-size: 1.05rem; font-weight: 300;
        }
        .testimonial-grid {
            display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px;
            max-width: 1000px; margin: 0 auto;
        }
        .testimonial-card {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-radius: var(--radius); padding: 32px 28px;
            border: 1px solid var(--border-glass);
            transition: all 0.3s;
        }
        .testimonial-card:hover {
            border-color: var(--border-glass-hover);
            transform: translateY(-4px);
            box-shadow: 0 16px 48px rgba(0, 0, 0, 0.25);
        }
        .testimonial-card .stars { font-size: 0.85rem; margin-bottom: 16px; letter-spacing: 2px; }
        .testimonial-card p {
            font-size: 0.95rem; color: var(--text-secondary); line-height: 1.75;
            font-weight: 300; margin-bottom: 24px;
        }
        .testimonial-author { display: flex; align-items: center; gap: 12px; }
        .testimonial-avatar {
            width: 42px; height: 42px; border-radius: 12px;
            background: linear-gradient(135deg, var(--accent), var(--accent-secondary));
            display: flex; align-items: center; justify-content: center;
            font-size: 0.85rem; font-weight: 700; color: #fff;
        }
        .testimonial-name { font-size: 0.88rem; font-weight: 600; color: var(--text-primary); }
        .testimonial-role { font-size: 0.78rem; color: var(--text-muted); font-weight: 400; }

        /* ========== PRICING ========== */
        .pricing-section {
            padding: 120px 24px; position: relative;
        }
        .pricing-section::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .section-label {
            text-align: center; font-size: 0.88rem; text-transform: uppercase;
            letter-spacing: 0.18em; color: var(--accent); font-weight: 600; margin-bottom: 18px;
        }
        .section-title {
            text-align: center; font-size: clamp(2.2rem, 4vw, 3.2rem); font-weight: 800;
            margin-bottom: 24px; color: var(--text-primary); letter-spacing: -0.03em; line-height: 1.15;
        }
        .section-sub {
            text-align: center; color: var(--text-secondary); max-width: 660px;
            margin: 0 auto 72px; line-height: 1.8; font-size: 1.15rem; font-weight: 300;
        }
        .section-divider {
            width: 48px; height: 2px; margin: 0 auto 20px; border-radius: 2px;
            background: linear-gradient(90deg, var(--accent), var(--accent-secondary));
        }
        .pricing-grid {
            display: grid; grid-template-columns: 1fr 1fr; gap: 28px;
            max-width: 820px; margin: 0 auto;
        }
        .pricing-card {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-radius: var(--radius); padding: 40px 32px;
            border: 1px solid var(--border-glass);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .pricing-card:hover {
            border-color: var(--border-glass-hover);
            box-shadow: 0 16px 48px rgba(0, 0, 0, 0.25);
            transform: translateY(-4px);
        }
        .pricing-phase {
            font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.12em;
            color: var(--accent); font-weight: 600; margin-bottom: 10px;
        }
        .pricing-card h3 { font-size: 1.3rem; margin-bottom: 6px; color: var(--text-primary); font-weight: 700; }
        .pricing-card .pricing-from {
            font-size: 1.6rem; font-weight: 800; color: var(--accent); margin-bottom: 12px;
        }
        .pricing-card .pricing-from span {
            font-size: 0.85rem; font-weight: 400; color: var(--text-muted);
        }
        .pricing-card .pricing-desc { font-size: 0.92rem; color: var(--text-muted); line-height: 1.75; margin-bottom: 24px; font-weight: 300; }
        .pricing-includes { list-style: none; padding: 0; }
        .pricing-includes li {
            font-size: 0.9rem; color: var(--text-secondary); padding: 7px 0 7px 28px;
            position: relative; line-height: 1.6; font-weight: 400;
        }
        .pricing-includes li::before {
            content: '✓'; position: absolute; left: 0; color: var(--accent-secondary);
            font-weight: 700; font-size: 0.85rem;
        }
        .pricing-note {
            text-align: center; max-width: 640px; margin: 48px auto 0;
            color: var(--text-muted); font-size: 0.92rem; line-height: 1.8; font-weight: 300;
        }
        .pricing-note a { color: var(--accent); font-weight: 600; transition: color 0.2s; }
        .pricing-note a:hover { color: #2aa8ff; }

        /* ========== FINAL CTA ========== */
        .cta-section { padding: 100px 24px; }
        .cta {
            text-align: center; border-radius: 24px; max-width: 860px;
            margin: 0 auto; padding: 80px 48px; position: relative; overflow: hidden;
            background: var(--bg-glass);
            backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-glass);
        }
        .cta::before {
            content: ''; position: absolute; inset: 0;
            background: radial-gradient(ellipse at 50% 100%, rgba(17, 153, 250, 0.1) 0%, transparent 60%);
        }
        .cta::after {
            content: ''; position: absolute; top: -50%; left: -50%; width: 200%; height: 200%;
            background: radial-gradient(circle at 30% 80%, rgba(139, 92, 246, 0.04), transparent 40%),
                        radial-gradient(circle at 70% 20%, rgba(0, 158, 126, 0.04), transparent 40%);
            pointer-events: none;
        }
        .cta > * { position: relative; z-index: 1; }
        .cta h2 {
            font-size: clamp(1.8rem, 3.5vw, 2.6rem); margin-bottom: 16px;
            font-weight: 800; letter-spacing: -0.03em;
        }
        .cta p {
            color: var(--text-secondary); margin-bottom: 40px; font-size: 1.1rem;
            line-height: 1.8; max-width: 500px; margin-left: auto; margin-right: auto;
            font-weight: 300;
        }
        .cta-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }

        /* ========== FOOTER ========== */
        .site-footer {
            background: var(--bg-deep); color: var(--text-muted); padding: 72px 24px 40px;
            border-top: 1px solid var(--border-glass);
        }
        .footer-grid {
            display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 48px; max-width: 1000px; margin: 0 auto 48px;
        }
        .footer-brand { font-size: 1.3rem; font-weight: 800; color: #fff; margin-bottom: 12px; }
        .footer-brand-desc { font-size: 0.88rem; line-height: 1.8; color: var(--text-muted); max-width: 280px; }
        .footer-col h4 {
            font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.14em;
            color: var(--text-muted); font-weight: 600; margin-bottom: 16px;
        }
        .footer-col a {
            display: block; font-size: 0.88rem; color: var(--text-muted);
            padding: 5px 0; transition: color 0.2s;
        }
        .footer-col a:hover { color: #fff; }
        .footer-bottom {
            text-align: center; padding-top: 32px;
            border-top: 1px solid var(--border-glass);
            font-size: 0.82rem; color: var(--text-muted);
            max-width: 1000px; margin: 0 auto;
        }

        /* ========== BACK TO TOP ========== */
        .back-to-top {
            position: fixed; bottom: 32px; right: 32px;
            width: 46px; height: 46px; border-radius: 14px;
            background: var(--bg-glass); color: var(--accent);
            backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--border-glass);
            display: flex; align-items: center; justify-content: center;
            font-size: 1.2rem; cursor: pointer;
            opacity: 0; transform: translateY(12px);
            transition: all 0.3s; z-index: 90;
        }
        .back-to-top.show { opacity: 1; transform: translateY(0); }
        .back-to-top:hover {
            background: var(--accent-dim); border-color: rgba(17, 153, 250, 0.3);
            transform: translateY(-2px);
        }

        /* ========== RESPONSIVE ========== */
        @media (max-width: 768px) {
            .hero { padding: 100px 20px 40px; min-height: auto; text-align: center; }
            .hero-inner { grid-template-columns: 1fr; }
            .hero-text { align-items: center; }
            .hero .hero-sub { margin: 0 auto 20px; }
            .hero .hero-explainer { margin: 0 auto 32px; }
            .hero h1 { font-size: clamp(2.4rem, 9vw, 3.2rem); }
            .hero-buttons { flex-direction: column; align-items: center; justify-content: center; }
            .hero-image-inner { transform: none; }
            .hero-video-wrap { margin-bottom: 40px; }
            .who-grid { grid-template-columns: 1fr; }
            .examples-grid { grid-template-columns: 1fr; }
            .journey-step { padding: 80px 20px; }
            .journey-inner { grid-template-columns: 1fr; gap: 40px; }
            .journey-step:nth-child(even) .journey-inner { direction: ltr; }
            .journey-visual { order: -1; }
            .journey-icon-box { aspect-ratio: 16/9; font-size: 3.5rem; }
            .testimonial-grid { grid-template-columns: 1fr; }
            .pricing-grid { grid-template-columns: 1fr; }
            .cta { padding: 56px 24px; border-radius: 16px; }
            .footer-grid { grid-template-columns: 1fr 1fr; gap: 32px; }
            .back-to-top { bottom: 20px; right: 20px; width: 40px; height: 40px; }
        }
        @media (max-width: 480px) {
            .footer-grid { grid-template-columns: 1fr; gap: 24px; }
        }
    </style>
</head>
<body>

<!-- ==================== NAV ==================== -->
<nav>
    <a href="/" class="nav-brand">◆ Morti</a>
    <button class="hamburger" onclick="this.nextElementSibling.classList.toggle('open')" aria-label="Menu">☰</button>
    <div class="nav-links">
        <div class="nav-dropdown">
            <a href="/#products" class="nav-text nav-dropdown-trigger<%= typeof currentPage !== 'undefined' && (currentPage === 'products' || currentPage === 'ai-readiness') ? ' active' : '' %>">Products <span class="nav-arrow">▾</span></a>
            <div class="nav-dropdown-menu">
                <a href="/#products" class="nav-dropdown-item">Morti — Talk, Automate</a>
                <a href="/ai-readiness" class="nav-dropdown-item">AI Readiness Review</a>
            </div>
        </div>
        <a href="/about" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'about' ? ' active' : '' %>">About</a>
        <a href="/#pricing" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'pricing' ? ' active' : '' %>">Pricing</a>
        <a href="/contact" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'contact' ? ' active' : '' %>">Contact</a>
        <% if (isLoggedIn) { %>
            <div class="nav-dropdown">
                <a href="/profile" class="nav-cta nav-dropdown-trigger"><%= userName || 'My Account' %> <span class="nav-arrow">▾</span></a>
                <div class="nav-dropdown-menu">
                    <a href="<%= dashboardUrl %>" class="nav-dropdown-item">Dashboard</a>
                    <a href="/profile" class="nav-dropdown-item">Profile</a>
                    <a href="/logout" class="nav-dropdown-item">Logout</a>
                </div>
            </div>
        <% } else { %>
            <a href="/login" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'login' ? ' active' : '' %>">Login</a>
            <a href="/signup" class="nav-cta">Get Started</a>
        <% } %>
    </div>
</nav>

<!-- ==================== HERO ==================== -->
<section class="hero">
    <div class="hero-grid"></div>
    <div class="hero-orb hero-orb-1"></div>
    <div class="hero-orb hero-orb-2"></div>
    <div class="hero-orb hero-orb-3"></div>
    <div class="hero-inner">
        <div class="hero-text">
            <h1>Talk. Automate.<br><span class="gradient-text">Done. Forever.</span></h1>
            <p class="hero-sub">Describe how a process works today — by voice or text. Morti deploys an AI agent that handles it going forward. 24/7, no mistakes, no chasing.</p>

            <div class="hero-explainer">
                <strong style="color: var(--text-primary); font-weight: 600;">What is Morti?</strong> An AI automation platform for Australian SMBs. Have a conversation about your business process, and we deploy an intelligent AI agent that runs it — ongoing, managed, and always improving. No technical knowledge required. Just describe how the work gets done today.
            </div>

            <div class="hero-buttons">
                <a href="/signup" class="btn btn-primary btn-lg">Start a Conversation</a>
                <a href="#how-it-works" class="btn btn-glass btn-lg">See How It Works</a>
            </div>
        </div>

        <div class="hero-image-wrap">
            <div class="hero-image-inner">
                <!-- Fake dashboard "screenshot" -->
                <div class="hero-app">
                    <div class="hero-app-nav">
                        <div class="hero-app-logo">
                            <span style="font-size:18px;margin-right:6px;">⚡</span> <span style="font-weight:700;font-size:15px;color:#f0f4f8;">Morti</span>
                            <span style="font-weight:400;font-size:13px;color:rgba(240,244,248,0.5);margin-left:4px;">Projects</span>
                        </div>
                        <div class="hero-app-nav-links">
                            <span class="active">Dashboard</span>
                            <span>Automations</span>
                            <span>Analytics</span>
                            <span>Settings</span>
                        </div>
                        <div class="hero-app-nav-right">
                            <span class="hero-app-pill">3 Active</span>
                            <span class="hero-app-avatar">LM</span>
                        </div>
                    </div>
                    <div class="hero-app-body">
                        <div class="hero-app-sidebar">
                            <div class="hero-app-stat">
                                <div class="hero-app-stat-label">Automations Running</div>
                                <div class="hero-app-stat-value">12</div>
                                <div class="hero-app-stat-change positive">+3 this week</div>
                            </div>
                            <div class="hero-app-stat">
                                <div class="hero-app-stat-label">Hours Saved</div>
                                <div class="hero-app-stat-value">847</div>
                                <div class="hero-app-stat-change positive">+62 this month</div>
                            </div>
                            <div class="hero-app-stat">
                                <div class="hero-app-stat-label">Tasks Completed</div>
                                <div class="hero-app-stat-value">3,241</div>
                                <div class="hero-app-stat-change positive">99.2% success</div>
                            </div>
                            <div class="hero-app-recent">
                                <div class="hero-app-recent-title">Recent Activity</div>
                                <div class="hero-app-recent-item"><span class="dot green"></span>Invoice approval — <em>2m ago</em></div>
                                <div class="hero-app-recent-item"><span class="dot blue"></span>Onboarding flow — <em>18m ago</em></div>
                                <div class="hero-app-recent-item"><span class="dot purple"></span>Weekly report sent — <em>1h ago</em></div>
                                <div class="hero-app-recent-item"><span class="dot green"></span>Lead sync to CRM — <em>2h ago</em></div>
                            </div>
                        </div>
                        <div class="hero-app-main">
                            <div class="hero-app-chart-header">
                                <span style="font-weight:600;color:#f0f4f8;">Automation Performance</span>
                                <div class="hero-app-chart-tabs">
                                    <span class="active">24H</span><span>1W</span><span>1M</span><span>6M</span>
                                </div>
                            </div>
                            <div class="hero-app-chart">
                                <svg viewBox="0 0 600 180" preserveAspectRatio="none" style="width:100%;height:100%;">
                                    <defs>
                                        <linearGradient id="chartGrad" x1="0" y1="0" x2="0" y2="1">
                                            <stop offset="0%" stop-color="#1199fa" stop-opacity="0.3"/>
                                            <stop offset="100%" stop-color="#1199fa" stop-opacity="0"/>
                                        </linearGradient>
                                    </defs>
                                    <path d="M0,140 C30,135 60,130 100,120 C140,110 170,125 210,105 C250,85 280,90 320,70 C360,50 400,55 440,35 C480,20 520,25 560,15 L600,10 L600,180 L0,180Z" fill="url(#chartGrad)"/>
                                    <path d="M0,140 C30,135 60,130 100,120 C140,110 170,125 210,105 C250,85 280,90 320,70 C360,50 400,55 440,35 C480,20 520,25 560,15 L600,10" fill="none" stroke="#1199fa" stroke-width="2.5"/>
                                </svg>
                            </div>
                            <div class="hero-app-projects">
                                <div class="hero-app-project-row">
                                    <span class="dot green"></span>
                                    <span class="hero-app-project-name">Invoice Approval Pipeline</span>
                                    <span class="hero-app-project-status running">Running</span>
                                    <span class="hero-app-project-metric">↑ 142 tasks/wk</span>
                                </div>
                                <div class="hero-app-project-row">
                                    <span class="dot blue"></span>
                                    <span class="hero-app-project-name">New Starter Onboarding</span>
                                    <span class="hero-app-project-status running">Running</span>
                                    <span class="hero-app-project-metric">↑ 23 tasks/wk</span>
                                </div>
                                <div class="hero-app-project-row">
                                    <span class="dot purple"></span>
                                    <span class="hero-app-project-name">Weekly Ops Report</span>
                                    <span class="hero-app-project-status running">Running</span>
                                    <span class="hero-app-project-metric">Every Monday</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== WHO IS THIS FOR ==================== -->
<section class="who-section">
    <div class="container">
        <div class="section-label fade-in">Who Is This For</div>
        <div class="section-divider fade-in"></div>
        <h2 class="section-title fade-in">Built for business people, not developers</h2>
        <p class="section-sub fade-in">If you manage people, operations, or a business — your AI agent handles the repetitive work so your team can focus on what actually matters.</p>
        <div class="who-grid">
            <div class="who-card fade-in">
                <div class="who-icon">📋</div>
                <h3>Operations Managers</h3>
                <p>You know exactly which processes waste time. Describe them to Morti and get an AI agent that handles the busywork — no six-month IT project required.</p>
            </div>
            <div class="who-card fade-in fade-in-delay-1">
                <div class="who-icon">🏢</div>
                <h3>Business Owners</h3>
                <p>You're wearing too many hats. Your AI agent takes the repetitive work off your plate — like a tireless team member who handles processes 24/7.</p>
            </div>
            <div class="who-card fade-in fade-in-delay-2">
                <div class="who-icon">👥</div>
                <h3>Team Leaders</h3>
                <p>Your team is stuck doing the same thing every week. An AI agent handles the heavy lifting so your people can focus on higher-value work.</p>
            </div>
        </div>
    </div>
</section>

<!-- ==================== WHAT WE AUTOMATE ==================== -->
<section id="products" class="examples-section">
    <div class="container">
        <div class="section-label fade-in">What We Automate</div>
        <div class="section-divider fade-in"></div>
        <h2 class="section-title fade-in">Real problems your AI agent solves</h2>
        <p class="section-sub fade-in">These are the kinds of things our clients used to do manually. Now their AI agent handles it.</p>
        <div class="examples-grid">
            <div class="example-item fade-in">
                <div class="example-icon">📄</div>
                <div class="example-text">
                    <strong>Invoice &amp; approval workflows</strong>
                    <span>Your AI agent receives invoices, routes them for approval, and pushes payment — no one chases signatures.</span>
                </div>
            </div>
            <div class="example-item fade-in fade-in-delay-1">
                <div class="example-icon">🧑‍💼</div>
                <div class="example-text">
                    <strong>Employee onboarding</strong>
                    <span>Your AI agent handles new starter onboarding — accounts created, docs sent, training scheduled. Day one, sorted.</span>
                </div>
            </div>
            <div class="example-item fade-in">
                <div class="example-icon">📊</div>
                <div class="example-text">
                    <strong>Reporting &amp; dashboards</strong>
                    <span>Your AI agent pulls data from multiple systems, compiles the report, and delivers it to your inbox every Monday morning.</span>
                </div>
            </div>
            <div class="example-item fade-in fade-in-delay-1">
                <div class="example-icon">📬</div>
                <div class="example-text">
                    <strong>Lead capture &amp; follow-up</strong>
                    <span>Your AI agent catches every enquiry — CRM updated, follow-up sent, task assigned — in seconds, 24/7.</span>
                </div>
            </div>
            <div class="example-item fade-in">
                <div class="example-icon">🔄</div>
                <div class="example-text">
                    <strong>System integrations</strong>
                    <span>Your AI agent keeps your accounting, CRM, email, and spreadsheets in sync — no manual data entry.</span>
                </div>
            </div>
            <div class="example-item fade-in fade-in-delay-1">
                <div class="example-icon">📝</div>
                <div class="example-text">
                    <strong>Document generation</strong>
                    <span>Your AI agent assembles proposals, contracts, and reports from templates and data — no copy-pasting.</span>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== THE JOURNEY ==================== -->
<div id="how-it-works" class="journey">

    <!-- Step 1: Talk -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">1</div>
                <div class="journey-label">Describe The Problem</div>
                <h2>Just tell us what's slowing you down.</h2>
                <p>No forms. No jargon. Just have a conversation — by voice or text — about the process you want an AI agent to handle. Morti asks the right follow-up questions to understand the full picture.</p>
                <p class="journey-example">"Every week I spend half a day copying data from our CRM into a spreadsheet for the Monday meeting."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step1-talk.webp" alt="Describe the problem" loading="lazy"></div>
            </div>
        </div>
    </div>

    <!-- Step 2: Requirements -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">2</div>
                <div class="journey-label">We Capture Everything</div>
                <h2>Your conversation becomes a structured brief.</h2>
                <p>While you talk, Morti captures every detail — goals, edge cases, systems involved, who does what. You get a clear, organised requirements document you can review and edit.</p>
                <p class="journey-example">"I didn't even think of half these edge cases — Morti caught them all from our conversation."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step2-requirements.webp" alt="Requirements captured" loading="lazy"></div>
            </div>
        </div>
    </div>

    <!-- Step 3: Design -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">3</div>
                <div class="journey-label">Solution Design</div>
                <h2>We design your AI agent. You approve it.</h2>
                <p>Morti produces a clear design showing exactly what your AI agent will do, what systems it connects to, and how it works. Written in plain language — not developer-speak.</p>
                <p class="journey-example">"For the first time, I could actually understand the technical plan before it was built."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step3-design.webp" alt="Solution design" loading="lazy"></div>
            </div>
        </div>
    </div>

    <!-- Step 4: Proposal -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">4</div>
                <div class="journey-label">Clear Pricing</div>
                <h2>A fixed-price proposal. No surprises.</h2>
                <p>You get a professional proposal with clear deliverables, a fixed price, and a timeline — before any work starts. Share it with your team, your board, or your accountant.</p>
                <p class="journey-example">"I knew exactly what I was paying for before I said yes. That's rare."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step4-proposal.webp" alt="Fixed-price proposal" loading="lazy"></div>
            </div>
        </div>
    </div>

    <!-- Step 5: Build -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">5</div>
                <div class="journey-label">We Deploy It</div>
                <h2>Approve and your AI agent gets to work.</h2>
                <p>Morti builds and deploys your AI agent on a dedicated instance. You get regular progress updates and a working agent — typically in days, not months.</p>
                <p class="journey-example">"It was running in production within two weeks. Our last IT project took eight months."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step5-build.webp" alt="Build in progress" loading="lazy"></div>
            </div>
        </div>
    </div>

    <!-- Step 6: Maintain -->
    <div class="journey-step">
        <div class="journey-inner">
            <div class="journey-content fade-in">
                <div class="journey-num">6</div>
                <div class="journey-label">We Keep It Running</div>
                <h2>Your AI agent: managed, monitored, always on.</h2>
                <p>Your AI agent doesn't get handed off and forgotten. We monitor it, fine-tune it, and improve it over time. You focus on your business — your agent handles the process.</p>
                <p class="journey-example">"I haven't thought about it once since launch. It just works."</p>
            </div>
            <div class="journey-visual fade-in fade-in-delay-1">
                <div class="journey-icon-box"><img src="/images/journey/step6-maintain.webp" alt="Ongoing management" loading="lazy"></div>
            </div>
        </div>
    </div>
</div>

<!-- ==================== EXAMPLE WORKFLOWS ==================== -->
<section class="social-proof">
    <div class="container">
        <div class="section-label fade-in">Example Workflows</div>
        <h2 class="fade-in">See what your AI agent does</h2>
        <p class="section-sub fade-in">Real AI agents, running live for Australian businesses right now.</p>
        <div class="testimonial-grid">
            <div class="testimonial-card fade-in">
                <div style="font-size:2rem;margin-bottom:16px;">📄→✅→💰</div>
                <h3 style="font-size:1.05rem;font-weight:700;margin-bottom:10px;color:var(--text-primary);">Invoice Approval Pipeline</h3>
                <p>Supplier invoice lands in the inbox. Your AI agent extracts the details, matches it to a PO, routes it to the right approver based on amount and department, and pushes it to Xero once approved. What took 2 hours a day now takes zero.</p>
                <div style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border-glass);">
                    <span style="font-size:0.78rem;color:var(--accent);font-weight:600;">CONNECTS</span>
                    <span style="font-size:0.82rem;color:var(--text-muted);display:block;margin-top:4px;">Gmail · Google Sheets · Xero · Slack</span>
                </div>
            </div>
            <div class="testimonial-card fade-in fade-in-delay-1">
                <div style="font-size:2rem;margin-bottom:16px;">🧑‍💼→📧→🗂️</div>
                <h3 style="font-size:1.05rem;font-weight:700;margin-bottom:10px;color:var(--text-primary);">New Starter Onboarding</h3>
                <p>HR marks a candidate as hired. Your AI agent creates their Google Workspace account, sends the welcome pack, assigns training modules, notifies their manager, and adds them to the right Slack channels. Day one, sorted.</p>
                <div style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border-glass);">
                    <span style="font-size:0.78rem;color:var(--accent);font-weight:600;">CONNECTS</span>
                    <span style="font-size:0.82rem;color:var(--text-muted);display:block;margin-top:4px;">Google Workspace · Slack · Notion · Gmail</span>
                </div>
            </div>
            <div class="testimonial-card fade-in fade-in-delay-2">
                <div style="font-size:2rem;margin-bottom:16px;">📊→📧→👀</div>
                <h3 style="font-size:1.05rem;font-weight:700;margin-bottom:10px;color:var(--text-primary);">Weekly Ops Report</h3>
                <p>Every Monday at 7am, your AI agent pulls sales from Shopify, support tickets from Zendesk, and financials from Xero. Compiles a single-page summary with trends and flags, and emails it to the leadership team. Your team focuses on decisions, not data wrangling.</p>
                <div style="margin-top:16px;padding-top:16px;border-top:1px solid var(--border-glass);">
                    <span style="font-size:0.78rem;color:var(--accent);font-weight:600;">CONNECTS</span>
                    <span style="font-size:0.82rem;color:var(--text-muted);display:block;margin-top:4px;">Shopify · Zendesk · Xero · Gmail</span>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== AI READINESS REVIEW ==================== -->
<!-- ==================== PRICING ==================== -->
<section id="pricing" class="pricing-section">
    <div class="section-label fade-in">Pricing</div>
    <div class="section-divider fade-in"></div>
    <h2 class="section-title fade-in">Simple, transparent pricing</h2>
    <p class="section-sub fade-in">You pay for outcomes, not hours. No lock-in. You own your IP. Always.</p>
    <div class="pricing-grid">
        <div class="pricing-card fade-in">
            <div class="pricing-phase">Upfront</div>
            <h3>Build Fee</h3>
            <div class="pricing-from">From $2,500 <span>+ GST</span></div>
            <p class="pricing-desc">Fixed price, scoped before we start. Covers everything from discovery to a running automation.</p>
            <ul class="pricing-includes">
                <li>Voice/text requirements capture</li>
                <li>Solution design &amp; proposal</li>
                <li>Full build, testing &amp; deployment</li>
                <li>Your own dedicated instance</li>
                <li>Handover documentation included</li>
            </ul>
        </div>
        <div class="pricing-card fade-in fade-in-delay-1">
            <div class="pricing-phase">Ongoing</div>
            <h3>Management Fee</h3>
            <div class="pricing-from">From $299/mo <span>+ GST</span></div>
            <p class="pricing-desc">We keep it running, monitored, and improving. Priced as a fraction of the value it delivers.</p>
            <ul class="pricing-includes">
                <li>24/7 hosting &amp; monitoring</li>
                <li>Bug fixes &amp; maintenance</li>
                <li>Feature updates &amp; improvements</li>
                <li>Priority support</li>
                <li>Cancel anytime — full export included</li>
            </ul>
        </div>
    </div>
    <p class="pricing-note fade-in">
        Every project is different, so we quote based on what you actually need. The discovery conversation is free — no commitment, no pressure.
        <br><br>
        <a href="/signup">Sign up to start a conversation</a>, or <a href="/contact">get in touch</a> to ask questions first.
    </p>
</section>

<!-- ==================== SEE IT IN ACTION ==================== -->
<section class="video-section">
    <div class="container" style="text-align:center;">
        <div class="section-label fade-in">Morti. Just Works.</div>
        <div class="section-divider fade-in"></div>
        <div class="video-wrap fade-in">
            <div class="video-container">
                <video playsinline preload="metadata" poster="/morti-poster.jpg" id="hero-vid">
                    <source src="/morti.mp4" type="video/mp4">
                </video>
                <div class="video-play-overlay" id="play-overlay" onclick="playHeroVideo()">
                    <div class="video-play-btn"></div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== FINAL CTA ==================== -->
<section class="cta-section">
    <div class="cta fade-in">
        <h2>Still doing it manually?</h2>
        <p>Tell us what's eating your time. The conversation takes 20 minutes. Your AI agent saves you hours every week.</p>
        <div class="cta-buttons">
            <a href="/signup" class="btn btn-primary btn-lg">Start a Conversation</a>
            <a href="/contact" class="btn btn-glass">Ask Us Anything</a>
        </div>
    </div>
</section>

<!-- ==================== FOOTER ==================== -->
<footer class="site-footer">
    <div class="footer-grid">
        <div>
            <div class="footer-brand">◆ Morti</div>
            <p class="footer-brand-desc">AI agents for Australian SMBs. Describe your process, we deploy an AI agent that handles it. No technical knowledge required.</p>
        </div>
        <div class="footer-col">
            <h4>Product</h4>
            <a href="#how-it-works">How It Works</a>
            <a href="#pricing">Pricing</a>
            <a href="#products">What We Automate</a>
        </div>
        <div class="footer-col">
            <h4>Company</h4>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <% if (isLoggedIn) { %>
                <a href="<%= dashboardUrl %>">Dashboard</a>
                <a href="/profile">My Account</a>
            <% } else { %>
                <a href="/signup">Get Started</a>
                <a href="/login">Customer Login</a>
            <% } %>
        </div>
        <div class="footer-col">
            <h4>Contact</h4>
            <a href="mailto:info@morti.com.au">Email Us</a>
            <span style="display:block;font-size:0.88rem;padding:5px 0;">Melbourne, Australia</span>
            <a href="https://morti.com.au">morti.com.au</a>
        </div>
    </div>
    <div class="footer-bottom">
        &copy; <%= new Date().getFullYear() %> Morti Pty Ltd · ABN 15 687 741 797 · Australia
    </div>
</footer>

<!-- Back to top -->
<button class="back-to-top" onclick="window.scrollTo({top:0,behavior:'smooth'})" aria-label="Back to top">↑</button>

<script>
// Scroll fade-in
const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); } });
}, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

// Nav shadow + back to top
const btt = document.querySelector('.back-to-top');
window.addEventListener('scroll', () => {
    const nav = document.querySelector('nav');
    if (nav) nav.classList.toggle('scrolled', window.scrollY > 20);
    btt.classList.toggle('show', window.scrollY > 600);
});

// Mobile dropdown toggle
document.querySelectorAll('.nav-dropdown-trigger').forEach(trigger => {
    trigger.addEventListener('click', function(e) {
        if (window.innerWidth <= 768) {
            e.preventDefault();
            this.closest('.nav-dropdown').classList.toggle('open');
        }
    });
});

// Hero video play overlay
function playHeroVideo() {
    const vid = document.getElementById('hero-vid');
    const overlay = document.getElementById('play-overlay');
    vid.controls = true;
    vid.play();
    overlay.classList.add('hidden');
}
document.getElementById('hero-vid').addEventListener('pause', () => {
    document.getElementById('play-overlay').classList.remove('hidden');
});
document.getElementById('hero-vid').addEventListener('play', () => {
    document.getElementById('play-overlay').classList.add('hidden');
});
</script>

</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About — Morti</title>
    <meta name="description" content="Morti is an Australian AI consultancy helping SMBs turn ideas into working automations — no lock-in, no overhead.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #050d1a;
            --bg-primary: #0a1628;
            --bg-card: #0f1d32;
            --bg-card-hover: #142640;
            --bg-glass: rgba(15, 29, 50, 0.6);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa;
            --accent-glow: rgba(17, 153, 250, 0.4);
            --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #00d4aa;
            --accent-secondary-glow: rgba(0, 212, 170, 0.3);
            --accent-purple: #8b5cf6;
            --accent-purple-glow: rgba(139, 92, 246, 0.3);
            --text-primary: #f0f4f8;
            --text-secondary: rgba(240, 244, 248, 0.7);
            --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px;
            --radius-sm: 10px;
            --radius-pill: 50px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: var(--text-primary); background: var(--bg-deep);
            overflow-x: hidden; font-weight: 400;
            -webkit-font-smoothing: antialiased;
        }
        a { text-decoration: none; color: inherit; }
        html { scroll-behavior: smooth; }

        /* ========== NAV ========== */
        nav {
            position: fixed; top: 0; width: 100%; z-index: 100;
            display: flex; align-items: center; justify-content: space-between;
            padding: 16px 40px;
            background: rgba(5, 13, 26, 0.8);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--border-glass);
            transition: all 0.3s;
        }
        nav.scrolled { background: rgba(5, 13, 26, 0.95); }
        .nav-brand { font-size: 1.3rem; font-weight: 800; color: #fff; letter-spacing: -0.02em; }
        .nav-links { display: flex; gap: 32px; align-items: center; }
        .nav-links a.nav-text {
            font-size: 0.88rem; color: var(--text-muted); transition: color 0.2s;
            font-weight: 500; letter-spacing: 0.01em;
        }
        .nav-links a.nav-text:hover { color: #fff; }
        .nav-links a.nav-text.active { color: var(--accent); }
        .nav-cta {
            background: var(--accent); color: #fff; padding: 10px 28px;
            border-radius: var(--radius-pill); font-size: 0.88rem; font-weight: 600;
            box-shadow: 0 0 16px var(--accent-glow); transition: all 0.3s;
        }
        .nav-cta:hover { background: #2aa8ff; box-shadow: 0 0 24px var(--accent-glow); transform: translateY(-1px); }
        .hamburger { display: none; background: none; border: none; font-size: 1.5rem; cursor: pointer; padding: 4px 8px; color: #fff; }
        @media (max-width: 768px) {
            nav { padding: 12px 20px; }
            .hamburger { display: block; }
            .nav-links {
                display: none; flex-direction: column; position: absolute; top: 100%;
                left: 0; right: 0; background: rgba(10, 22, 40, 0.98);
                backdrop-filter: blur(20px); padding: 16px 20px; gap: 4px;
                border-bottom: 1px solid var(--border-glass);
            }
            .nav-links.open { display: flex; }
            .nav-links a.nav-text { padding: 12px 0; font-size: 1rem; }
            .nav-cta { text-align: center; margin-top: 8px; display: block; }
        }

        /* ========== BUTTONS ========== */
        .btn {
            display: inline-flex; align-items: center; justify-content: center;
            padding: 14px 36px; border-radius: var(--radius-pill); font-size: 0.95rem;
            font-weight: 600; cursor: pointer; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: none; letter-spacing: -0.01em;
        }
        .btn-primary {
            background: var(--accent); color: #fff;
            box-shadow: 0 0 20px var(--accent-glow), 0 4px 12px rgba(0, 0, 0, 0.3);
        }
        .btn-primary:hover {
            background: #2aa8ff; transform: translateY(-2px);
            box-shadow: 0 0 32px var(--accent-glow), 0 8px 24px rgba(0, 0, 0, 0.4);
        }
        .btn-glass {
            background: rgba(255, 255, 255, 0.06); color: #fff;
            border: 1px solid rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
        }
        .btn-glass:hover {
            background: rgba(255, 255, 255, 0.12); border-color: rgba(255, 255, 255, 0.25);
            transform: translateY(-2px);
        }

        /* ========== HERO ========== */
        .about-hero {
            padding: 160px 40px 100px; text-align: center;
            background: var(--bg-deep); color: #fff; position: relative; overflow: hidden;
        }
        .about-hero::before {
            content: ''; position: absolute; inset: 0;
            background: radial-gradient(ellipse at 40% 80%, rgba(17,153,250,0.1) 0%, transparent 60%);
        }
        .hero-grid {
            position: absolute; inset: 0; opacity: 0.03;
            background-image:
                linear-gradient(rgba(255,255,255,0.5) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255,255,255,0.5) 1px, transparent 1px);
            background-size: 80px 80px;
        }
        .about-hero > * { position: relative; z-index: 1; }
        .about-hero h1 {
            font-size: clamp(2.4rem, 5vw, 3.2rem); font-weight: 900; margin-bottom: 20px;
            letter-spacing: -0.04em;
        }
        .about-hero h1 .gradient-text {
            background: linear-gradient(135deg, var(--accent) 0%, var(--accent-secondary) 50%, var(--accent-purple) 100%);
            -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
        }
        .about-hero p { font-size: 1.18rem; max-width: 600px; margin: 0 auto; color: var(--text-secondary); line-height: 1.75; font-weight: 300; }

        /* ========== CONTENT ========== */
        .about-content { max-width: 760px; margin: 0 auto; padding: 80px 40px; }
        .about-content h2 {
            font-size: 1.5rem; font-weight: 700; color: var(--text-primary);
            margin: 56px 0 16px; letter-spacing: -0.01em;
        }
        .about-content h2:first-child { margin-top: 0; }
        .section-divider-left {
            width: 48px; height: 2px; border-radius: 2px; margin-bottom: 20px;
            background: linear-gradient(90deg, var(--accent), var(--accent-secondary));
        }
        .about-content p { font-size: 1.02rem; color: var(--text-secondary); line-height: 1.85; margin-bottom: 16px; font-weight: 300; }

        .values-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; margin: 36px 0; }
        .value-card {
            background: var(--bg-glass); border-radius: var(--radius); padding: 28px 24px;
            border: 1px solid var(--border-glass); transition: all 0.3s;
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
        }
        .value-card:hover { transform: translateY(-2px); box-shadow: 0 8px 32px rgba(0,0,0,0.25); border-color: var(--border-glass-hover); }
        .value-icon {
            width: 44px; height: 44px; border-radius: 12px; margin-bottom: 12px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.3rem; background: var(--accent-dim);
            border: 1px solid rgba(17, 153, 250, 0.2);
        }
        .value-card h3 { font-size: 1rem; color: var(--text-primary); margin-bottom: 6px; font-weight: 700; }
        .value-card p { font-size: 0.88rem; color: var(--text-muted); line-height: 1.65; margin: 0; font-weight: 300; }

        .founder-section {
            background: var(--bg-glass); border-radius: var(--radius); padding: 40px 36px;
            margin: 40px 0; border: 1px solid var(--border-glass); transition: all 0.3s;
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
        }
        .founder-section:hover { box-shadow: 0 8px 32px rgba(0,0,0,0.25); border-color: var(--border-glass-hover); }
        .founder-section h3 { font-size: 1.2rem; font-weight: 700; color: var(--text-primary); margin-bottom: 4px; }
        .founder-section .role { font-size: 0.88rem; color: var(--accent); font-weight: 600; margin-bottom: 14px; }
        .founder-section p { font-size: 0.95rem; color: var(--text-secondary); line-height: 1.75; margin-bottom: 10px; font-weight: 300; }

        /* ========== CTA ========== */
        .cta-bar {
            text-align: center; padding: 100px 40px; position: relative;
        }
        .cta-bar::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
            background: linear-gradient(90deg, transparent, var(--border-glass), transparent);
        }
        .cta-inner {
            max-width: 860px; margin: 0 auto; padding: 80px 48px;
            background: var(--bg-glass); backdrop-filter: blur(24px); -webkit-backdrop-filter: blur(24px);
            border: 1px solid var(--border-glass); border-radius: 24px;
            position: relative; overflow: hidden;
        }
        .cta-inner::before {
            content: ''; position: absolute; inset: 0;
            background: radial-gradient(ellipse at 50% 100%, rgba(17, 153, 250, 0.1) 0%, transparent 60%);
        }
        .cta-inner > * { position: relative; z-index: 1; }
        .cta-bar h2 { font-size: 1.6rem; color: var(--text-primary); margin-bottom: 12px; font-weight: 700; letter-spacing: -0.01em; }
        .cta-bar p { color: var(--text-secondary); margin-bottom: 28px; font-weight: 300; }
        .cta-buttons { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }

        /* ========== FOOTER ========== */
        .site-footer {
            background: var(--bg-deep); color: var(--text-muted); padding: 72px 24px 40px;
            border-top: 1px solid var(--border-glass);
        }
        .footer-grid {
            display: grid; grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 48px; max-width: 1000px; margin: 0 auto 48px;
        }
        .footer-brand { font-size: 1.3rem; font-weight: 800; color: #fff; margin-bottom: 12px; }
        .footer-brand-desc { font-size: 0.88rem; line-height: 1.8; color: var(--text-muted); max-width: 280px; }
        .footer-col h4 {
            font-size: 0.72rem; text-transform: uppercase; letter-spacing: 0.14em;
            color: var(--text-muted); font-weight: 600; margin-bottom: 16px;
        }
        .footer-col a {
            display: block; font-size: 0.88rem; color: var(--text-muted);
            padding: 5px 0; transition: color 0.2s;
        }
        .footer-col a:hover { color: #fff; }
        .footer-bottom {
            text-align: center; padding-top: 32px;
            border-top: 1px solid var(--border-glass);
            font-size: 0.82rem; color: var(--text-muted);
            max-width: 1000px; margin: 0 auto;
        }

        /* ========== FADE IN ========== */
        .fade-in {
            opacity: 0; transform: translateY(24px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }
        .fade-in.visible { opacity: 1; transform: translateY(0); }
        .fade-in-delay-1 { transition-delay: 0.1s; }
        .fade-in-delay-2 { transition-delay: 0.2s; }

        @media (max-width: 768px) {
            .about-hero { padding: 120px 20px 70px; }
            .about-hero h1 { font-size: 2.2rem; }
            .about-content { padding: 48px 20px; }
            .values-grid { grid-template-columns: 1fr; }
            .cta-inner { padding: 56px 24px; border-radius: 16px; }
            .footer-grid { grid-template-columns: 1fr 1fr; gap: 32px; }
        }
        @media (max-width: 480px) { .footer-grid { grid-template-columns: 1fr; gap: 24px; } }
    </style>
</head>
<body>

<nav>
    <a href="/" class="nav-brand">◆ Morti</a>
    <button class="hamburger" onclick="this.nextElementSibling.classList.toggle('open')" aria-label="Menu">☰</button>
    <div class="nav-links">
        <a href="/#products" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'products' ? ' active' : '' %>">What We Do</a>
        <a href="/about" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'about' ? ' active' : '' %>">About</a>
        <a href="/#pricing" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'pricing' ? ' active' : '' %>">Pricing</a>
        <a href="/contact" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'contact' ? ' active' : '' %>">Contact</a>
        <a href="/login" class="nav-text<%= typeof currentPage !== 'undefined' && currentPage === 'login' ? ' active' : '' %>">Login</a>
        <a href="/signup" class="nav-cta">Get Started</a>
    </div>
</nav>

<section class="about-hero">
    <div class="hero-grid"></div>
    <h1>About <span class="gradient-text">Morti</span></h1>
    <p>An Australian AI consultancy turning business ideas into working automations — without the enterprise overhead or vendor lock-in.</p>
</section>

<div class="about-content">
    <h2 class="fade-in">Who we are</h2>
    <div class="section-divider-left fade-in"></div>
    <p class="fade-in">Morti is an Australian AI consultancy that works with small to medium businesses across the country. We specialise in taking complex business ideas and turning them into practical, running automations — from initial concept through to managed operation.</p>
    <p class="fade-in">We started Morti because we saw too many businesses either priced out of quality technology consulting, or locked into solutions they couldn't leave. We believe every business deserves access to leading-edge AI, delivered by people who actually listen — and who'll hand over the keys when you ask.</p>

    <h2 class="fade-in">The vision</h2>
    <div class="section-divider-left fade-in"></div>
    <p class="fade-in">Morti is building an AI-native consulting infrastructure. Not just advice — we build and run the solution. Our two services work together: <strong>Morti Projects</strong> handles requirements, design, and proposals; <strong>Mortomation</strong> builds and operates the automation. End-to-end, from idea to live system.</p>
    <p class="fade-in">We're not interested in creating dependency. Everything we build is exportable, documented, and portable. If you want to run your automation yourself or take it to another provider, we make that easy. We succeed when you stay because the service is good, not because you're stuck.</p>

    <h2 class="fade-in">What makes us different</h2>
    <div class="section-divider-left fade-in"></div>
    <div class="values-grid">
        <div class="value-card fade-in">
            <div class="value-icon">🔓</div>
            <h3>No lock-in, ever</h3>
            <p>Full export at any time. Workflow configs, documentation, setup guides. You own your IP — period.</p>
        </div>
        <div class="value-card fade-in fade-in-delay-1">
            <div class="value-icon">🤝</div>
            <h3>Handover capability</h3>
            <p>Request a complete handover whenever you want. Run it yourself, switch providers, or stay with us. Your call.</p>
        </div>
        <div class="value-card fade-in">
            <div class="value-icon">🧠</div>
            <h3>AI-native, not AI-bolted</h3>
            <p>We don't bolt AI onto old processes. We rethink how work gets done with AI at the core.</p>
        </div>
        <div class="value-card fade-in fade-in-delay-1">
            <div class="value-icon">👁️</div>
            <h3>Human oversight</h3>
            <p>AI designs, humans approve. No black boxes. You see and control what's being built.</p>
        </div>
        <div class="value-card fade-in">
            <div class="value-icon">🎯</div>
            <h3>Practical over perfect</h3>
            <p>We build MVPs that work and iterate from there. No over-engineering, no enterprise theatre.</p>
        </div>
        <div class="value-card fade-in fade-in-delay-1">
            <div class="value-icon">💬</div>
            <h3>Plain language</h3>
            <p>No jargon. We explain what we're building and why in terms that actually make sense.</p>
        </div>
    </div>

    <h2 class="fade-in">Our Team</h2>
    <div class="section-divider-left fade-in"></div>
    <div class="founder-section fade-in">
        <h3>Australian-Based Team</h3>
        <div class="role">AI Consulting & Managed Automation</div>
        <p>Our team works at the intersection of technology and business operations. We founded Morti to make AI consulting accessible to the businesses that need it most — SMBs that have clear ideas but need the technical expertise to make them real.</p>
        <p>We work directly with every client to ensure the solution actually solves the problem — not just looks good on paper.</p>
    </div>

    <h2 class="fade-in">Based in Australia, working nationwide</h2>
    <div class="section-divider-left fade-in"></div>
    <p class="fade-in">Morti Pty Ltd works with clients right across Australia. Most collaboration happens remotely through our platform, but we're always happy to meet in person when it makes sense.</p>
    <p class="fade-in">Contact: <a href="mailto:info@morti.com.au" style="color: var(--accent); font-weight: 500;">info@morti.com.au</a></p>
</div>

<section class="cta-bar">
    <div class="cta-inner fade-in">
        <h2>Got a project in mind?</h2>
        <p>No pressure, no sales pitch — just a conversation about what's possible.</p>
        <div class="cta-buttons">
            <a href="/signup" class="btn btn-primary">Get Started Free</a>
            <a href="/contact" class="btn btn-glass">Get in Touch</a>
        </div>
    </div>
</section>

<footer class="site-footer">
    <div class="footer-grid">
        <div>
            <div class="footer-brand">◆ Morti</div>
            <p class="footer-brand-desc">You talk, we build. AI-powered project delivery and managed automation for Australian small and medium businesses.</p>
        </div>
        <div class="footer-col">
            <h4>Product</h4>
            <a href="/#how-it-works">How It Works</a>
            <a href="/#pricing">Pricing</a>
            <a href="/#products">Products</a>
        </div>
        <div class="footer-col">
            <h4>Company</h4>
            <a href="/about">About</a>
            <a href="/contact">Contact</a>
            <a href="/signup">Get Started</a>
            <a href="/login">Customer Login</a>
        </div>
        <div class="footer-col">
            <h4>Contact</h4>
            <a href="mailto:info@morti.com.au">Email Us</a>
            <span style="display:block;font-size:0.88rem;padding:5px 0;">Australia</span>
            <a href="https://morti.com.au">morti.com.au</a>
        </div>
    </div>
    <div class="footer-bottom">
        &copy; <%= new Date().getFullYear() %> Morti Pty Ltd · ABN 15 687 741 797 · Australia
    </div>
</footer>

<script>
const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
window.addEventListener('scroll', () => {
    const nav = document.querySelector('nav');
    if (nav) nav.classList.toggle('scrolled', window.scrollY > 20);
});
</script>

</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Morti Projects</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-deep: #050d1a;
            --bg-primary: #0a1628;
            --bg-card: #0f1d32;
            --bg-glass: rgba(15, 29, 50, 0.6);
            --border-glass: rgba(255, 255, 255, 0.08);
            --border-glass-hover: rgba(255, 255, 255, 0.15);
            --accent: #1199fa;
            --accent-glow: rgba(17, 153, 250, 0.4);
            --accent-dim: rgba(17, 153, 250, 0.12);
            --accent-secondary: #00d4aa;
            --accent-purple: #8b5cf6;
            --text-primary: #f0f4f8;
            --text-secondary: rgba(240, 244, 248, 0.7);
            --text-muted: rgba(240, 244, 248, 0.4);
            --radius: 16px;
            --radius-pill: 50px;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--bg-deep); color: var(--text-primary); line-height: 1.6; -webkit-font-smoothing: antialiased; }
        a { text-decoration: none; color: inherit; }

        .main { max-width: 1200px; margin: 0 auto; padding: 24px; min-height: calc(100vh - 200px); }

        .card {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            border-radius: var(--radius);
            padding: 24px;
            margin-bottom: 20px;
            transition: border-color 0.3s;
        }
        .card:hover { border-color: var(--border-glass-hover); }
        .card h2 { margin-bottom: 16px; color: var(--text-primary); font-size: 1.3rem; font-weight: 700; }

        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 16px; margin-bottom: 28px; }
        .stat-card {
            background: var(--bg-glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--border-glass);
            border-radius: var(--radius);
            padding: 24px;
            text-align: center;
            transition: all 0.3s;
        }
        .stat-card:hover { border-color: var(--border-glass-hover); transform: translateY(-2px); }
        .stat-number { font-size: 2rem; font-weight: 800; color: var(--accent); margin-bottom: 4px; }
        .stat-label { font-size: 0.78rem; color: var(--text-muted); font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; }

        .table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        .table th, .table td { text-align: left; padding: 12px; border-bottom: 1px solid var(--border-glass); }
        .table th { background: rgba(15, 29, 50, 0.4); font-weight: 600; color: var(--text-muted); font-size: 0.78rem; text-transform: uppercase; letter-spacing: 0.06em; }
        .table tbody tr:hover { background: rgba(17, 153, 250, 0.05); }

        .btn {
            display: inline-flex; align-items: center; justify-content: center;
            padding: 10px 24px; border-radius: var(--radius-pill); font-size: 0.88rem;
            font-weight: 600; cursor: pointer; transition: all 0.3s; border: none;
            color: #fff; letter-spacing: -0.01em; text-decoration: none;
        }
        .btn-primary, .btn { background: var(--accent); box-shadow: 0 0 20px var(--accent-glow); }
        .btn-primary:hover, .btn:hover { background: #2aa8ff; transform: translateY(-2px); box-shadow: 0 0 32px var(--accent-glow); }
        .btn-sm { padding: 6px 14px; font-size: 0.78rem; }
        .btn-success { background: var(--accent-secondary); box-shadow: 0 0 16px rgba(0, 212, 170, 0.3); }
        .btn-success:hover { transform: translateY(-1px); box-shadow: 0 0 24px rgba(0, 212, 170, 0.4); }

        .empty-state { text-align: center; padding: 60px 20px; color: var(--text-muted); }
        .empty-state .emoji { font-size: 48px; margin-bottom: 16px; display: block; }
        .empty-state h3 { margin-bottom: 8px; color: var(--text-secondary); }

        @media (max-width: 768px) {
            .main { padding: 16px; }
            .card { padding: 16px; }
            .stats-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <%- include('../partials/admin-nav', { currentPage: 'dashboard', user: user }) %>

    <main class="main">
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number"><%= stats.totalCompanies %></div>
                <div class="stat-label">Companies</div>
            </div>
            <div class="stat-card">
                <div class="stat-number"><%= stats.totalUsers %></div>
                <div class="stat-label">Customer Accounts</div>
            </div>
            <div class="stat-card">
                <div class="stat-number"><%= stats.totalProjects %></div>
                <div class="stat-label">Active Projects</div>
            </div>
            <div class="stat-card">
                <div class="stat-number"><%= stats.totalSessions %></div>
                <div class="stat-label">Voice Sessions</div>
            </div>
        </div>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
            <div class="card">
                <h2>🏢 Recent Companies</h2>
                <% if (recentCustomers.length > 0) { %>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Company</th>
                                <th>Contact</th>
                                <th>Joined</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recentCustomers.slice(0, 6).forEach(customer => { %>
                                <tr>
                                    <td>
                                        <strong><%= customer.company %></strong><br>
                                        <small style="color: var(--text-muted);"><%= customer.name %></small>
                                    </td>
                                    <td style="color: var(--text-secondary);"><%= customer.email %></td>
                                    <td style="color: var(--text-secondary);"><%= melbDate(customer.created_at) %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                    <div style="text-align: center; margin-top: 16px;">
                        <a href="/admin/customers" class="btn btn-sm">View All Customers</a>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <span class="emoji">👥</span>
                        <h3>No customers yet</h3>
                        <p>Customer accounts will appear here</p>
                    </div>
                <% } %>
            </div>

            <% const reviewProjects = recentProjects.filter(p => p.status === 'completed' && p.design_review_requested); %>
            <% if (reviewProjects.length > 0) { %>
            <div class="card" style="border-left: 3px solid #f59e0b;">
                <h2>🔔 Design Review Needed</h2>
                <p style="color: var(--text-muted); font-size: 13px; margin-bottom: 12px;">These projects have been marked complete by customers and are waiting for design review.</p>
                <% reviewProjects.forEach(project => { %>
                    <div style="display:flex;align-items:center;justify-content:space-between;padding:10px 14px;background:rgba(245,158,11,0.1);border-radius:12px;margin-bottom:8px;border:1px solid rgba(245,158,11,0.2);">
                        <div>
                            <strong><%= project.name %></strong>
                            <span style="font-size:12px;color:var(--text-muted);margin-left:8px;"><%= project.user_name %> — <%= project.company %></span>
                        </div>
                        <a href="/admin/projects/<%= encodeId(project.id) %>" class="btn btn-sm" style="background:#f59e0b;box-shadow:0 0 16px rgba(245,158,11,0.3);">Review →</a>
                    </div>
                <% }) %>
            </div>
            <% } %>

            <div class="card">
                <h2>📊 Recent Projects</h2>
                <% if (recentProjects.length > 0) { %>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Project</th>
                                <th>Company</th>
                                <th>Sessions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recentProjects.slice(0, 6).forEach(project => { %>
                                <tr>
                                    <td>
                                        <strong><%= project.name %></strong><br>
                                        <% if (project.status === 'active') { %>
                                            <span style="color: var(--accent-secondary); font-size: 12px;">● Active</span>
                                        <% } else if (project.status === 'completed') { %>
                                            <span style="color: var(--text-muted); font-size: 12px;">● Completed</span>
                                            <% if (project.design_review_requested) { %>
                                                <span style="background:rgba(245,158,11,0.15);color:#f59e0b;font-size:10px;padding:2px 8px;border-radius:50px;margin-left:4px;font-weight:600;">Design Review</span>
                                            <% } %>
                                        <% } else { %>
                                            <span style="color: #ef4444; font-size: 12px;">● Archived</span>
                                        <% } %>
                                    </td>
                                    <td>
                                        <strong><%= project.company %></strong><br>
                                        <small style="color: var(--text-muted);"><%= project.user_name %></small>
                                    </td>
                                    <td style="color: var(--text-secondary);"><%= project.session_count || 0 %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                    <div style="text-align: center; margin-top: 16px;">
                        <a href="/admin/projects" class="btn btn-sm">View All Projects</a>
                    </div>
                <% } else { %>
                    <div class="empty-state">
                        <span class="emoji">📋</span>
                        <h3>No projects yet</h3>
                        <p>Customer projects will appear here</p>
                    </div>
                <% } %>
            </div>
        </div>

        <div class="card">
            <h2>🚀 Quick Actions</h2>
            <div style="display: flex; gap: 12px; flex-wrap: wrap;">
                <a href="/admin/customers" class="btn btn-success">➕ Add New Customer</a>
                <a href="/admin/projects" class="btn">📊 Manage All Projects</a>
                <a href="/admin/customers" class="btn">👥 Manage Customers</a>
                <a href="/admin/feature-requests" class="btn">💡 Feature Requests</a>
            </div>
        </div>
    </main>
</body>
</html>

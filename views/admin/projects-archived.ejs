<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Archived Projects - Morti Projects</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f0f2f5; line-height: 1.6; }
        .main { max-width: 1200px; margin: 0 auto; padding: 20px; }
        .card { background: white; border-radius: 12px; padding: 24px; margin-bottom: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
        .btn { display: inline-block; padding: 8px 16px; background: #667eea; color: white; text-decoration: none; border-radius: 8px; font-weight: 600; border: none; cursor: pointer; font-size: 13px; }
        .btn-sm { padding: 6px 12px; font-size: 12px; }
        .table { width: 100%; border-collapse: collapse; margin-top: 12px; }
        .table th, .table td { text-align: left; padding: 12px; border-bottom: 1px solid #e0e0e0; }
        .table th { background: #f8f9fa; font-weight: 600; color: #555; font-size: 13px; text-transform: uppercase; }
        .table tbody tr:hover { background: #f8f9ff; }
    </style>
</head>
<body>
    <%- include('../partials/admin-nav', { currentPage: 'projects', user: user }) %>
    <main class="main">
        <div class="card">
            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:20px;">
                <h2>📦 Archived Projects</h2>
                <a href="/admin/projects" class="btn" style="background:#6c757d;">← Back to Projects</a>
            </div>
            <% if (projects.length > 0) { %>
            <table class="table">
                <thead><tr><th>Project</th><th>Customer</th><th>Activity</th><th>Actions</th></tr></thead>
                <tbody>
                <% projects.forEach(p => { %>
                    <tr>
                        <td><strong><%= p.name %></strong><br><small style="color:#666;"><%= (p.description||'').substring(0,60) %></small></td>
                        <td><%= p.company %><br><small style="color:#666;"><%= p.user_name %></small></td>
                        <td style="font-size:13px;">💬 <%= p.session_count||0 %> sessions • 📎 <%= p.file_count||0 %> files</td>
                        <td>
                            <div style="display:flex;gap:8px;">
                                <a href="/admin/projects/<%= encodeId(p.id) %>" class="btn btn-sm">👁️ View</a>
                                <form method="POST" action="/admin/projects/<%= encodeId(p.id) %>/unarchive" style="margin:0;">
                                    <button type="submit" class="btn btn-sm" style="background:#28a745;color:white;">📦 Unarchive</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                <% }) %>
                </tbody>
            </table>
            <% } else { %>
            <div style="text-align:center;padding:40px;color:#999;">
                <div style="font-size:48px;margin-bottom:16px;">📦</div>
                <h3 style="color:#666;">No archived projects</h3>
            </div>
            <% } %>
        </div>
    </main>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= project.name %> - Proposal</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #f0f2f5; line-height: 1.6; }
        .main { max-width: 1000px; margin: 0 auto; padding: 20px; }
        .card { background: white; border-radius: 12px; padding: 24px; margin-bottom: 20px; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
        .btn { display: inline-block; padding: 10px 20px; background: #667eea; color: white; text-decoration: none; border-radius: 8px; font-weight: 600; border: none; cursor: pointer; font-size: 14px; }
        .btn:hover { background: #5a6fd8; }
        .breadcrumb { font-size: 13px; color: #666; margin-bottom: 20px; }
        .breadcrumb a { color: #667eea; text-decoration: none; }

        .fee-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin: 20px 0; }
        .fee-card { background: #f8fafc; border: 2px solid #e2e8f0; border-radius: 12px; padding: 24px; text-align: center; }
        .fee-card.highlight { border-color: #667eea; background: #f0f4ff; }
        .fee-label { font-size: 12px; text-transform: uppercase; letter-spacing: 1px; color: #667eea; font-weight: 700; margin-bottom: 8px; }
        .fee-amount { font-size: 2rem; font-weight: 800; color: #1e293b; margin-bottom: 8px; }
        .fee-amount small { font-size: 0.5em; color: #64748b; font-weight: 400; }
        .fee-desc { font-size: 0.85rem; color: #64748b; line-height: 1.5; }
        .fee-rationale { font-size: 0.8rem; color: #94a3b8; margin-top: 8px; font-style: italic; }

        .total-bar { background: linear-gradient(135deg, #667eea, #764ba2); color: white; border-radius: 12px; padding: 24px; display: flex; justify-content: space-around; text-align: center; margin: 20px 0; }
        .total-item h4 { font-size: 0.8rem; text-transform: uppercase; letter-spacing: 1px; opacity: 0.8; margin-bottom: 4px; }
        .total-item .amount { font-size: 1.5rem; font-weight: 800; }

        .value-card { background: #eff6ff; border: 2px solid #bfdbfe; border-radius: 12px; padding: 24px; margin: 20px 0; }
        .value-card h3 { color: #1e40af; margin-bottom: 16px; }
        .value-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #dbeafe; }
        .value-row:last-child { border: none; }
        .value-label { color: #1e40af; font-size: 0.9rem; }
        .value-text { color: #334155; font-size: 0.9rem; max-width: 60%; text-align: right; }
        .scenario-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-top: 16px; }
        .scenario { text-align: center; padding: 16px; border-radius: 8px; background: white; }
        .scenario-label { font-size: 0.75rem; text-transform: uppercase; color: #64748b; letter-spacing: 0.5px; }
        .scenario-amount { font-size: 1.3rem; font-weight: 800; color: #1e40af; margin-top: 4px; }

        .roi-card { background: #f0fdf4; border: 2px solid #bbf7d0; border-radius: 12px; padding: 24px; margin: 20px 0; }
        .roi-card h3 { color: #166534; margin-bottom: 12px; }
        .roi-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 16px; margin-bottom: 16px; }
        .roi-stat { text-align: center; padding: 16px; background: white; border-radius: 8px; }
        .roi-stat .label { font-size: 0.75rem; text-transform: uppercase; color: #166534; letter-spacing: 0.5px; }
        .roi-stat .val { font-size: 1.5rem; font-weight: 800; color: #166534; margin-top: 4px; }
        .roi-summary { color: #166534; font-size: 0.95rem; font-style: italic; padding-top: 12px; border-top: 1px solid #dcfce7; }

        .section { margin-bottom: 20px; }
        .section h3 { font-size: 1.1rem; color: #334155; margin-bottom: 10px; display: flex; align-items: center; gap: 8px; }
        .section p { color: #475569; font-size: 0.95rem; line-height: 1.7; white-space: pre-wrap; }
        .tag-list { display: flex; flex-wrap: wrap; gap: 8px; }
        .tag { background: #f1f5f9; padding: 6px 12px; border-radius: 20px; font-size: 0.85rem; color: #475569; }
        .tag.excl { background: #fef2f2; color: #991b1b; }
        .error-msg { background: #fff5f5; border: 2px solid #fed7d7; border-radius: 8px; padding: 12px 16px; color: #c53030; margin-bottom: 16px; }

        @media (max-width: 768px) {
            .fee-grid, .scenario-grid, .roi-grid { grid-template-columns: 1fr; }
            .total-bar { flex-direction: column; gap: 16px; }
            .value-row { flex-direction: column; gap: 4px; }
            .value-text { max-width: 100%; text-align: left; }
        }
    </style>
</head>
<body>

    <%- include('../partials/admin-nav', { currentPage: 'projects', user: user }) %>

<div class="main">
    <div class="breadcrumb">
        <a href="/admin">Dashboard</a> → <a href="/admin/projects">Projects</a>
                <a href="/admin/projects">Projects</a> → <a href="/admin/projects/<%= encodeId(project.id) %>"><%= project.name %></a> → Proposal
    </div>

    <% if (locals.query && locals.query.error) { %>
        <div class="error-msg"><%= locals.query.error %></div>
    <% } %>

    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px;">
        <div style="display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 12px;">
            <h2 style="margin: 0;">💰 Proposal — <%= project.name %></h2>
            <a href="/admin/projects/<%= encodeId(project.id) %>" class="btn btn-sm" style="background: #64748b;">← Back</a>
        </div>
        
        <!-- Action bar -->
        <div style="display: flex; flex-wrap: wrap; gap: 10px; margin-top: 16px; padding: 16px; background: #f8fafc; border-radius: 10px; border: 1px solid #e2e8f0;">
            <% if (design) { %>
            <form method="POST" action="/admin/projects/<%= encodeId(project.id) %>/generate-proposal" style="margin:0; display:flex; flex-wrap:wrap; gap:8px; align-items:center;" id="genForm">
                <div style="display:flex; gap:4px; align-items:center;">
                    <label style="font-size:12px; color:#64748b; font-weight:600;">Upfront</label>
                    <input type="number" name="discountUpfront" min="0" max="100" step="1" placeholder="%" style="width:60px; padding:6px 8px; border:2px solid #e2e8f0; border-radius:6px; font-size:13px; text-align:center;" title="Upfront fee discount %" value="<%= proposal && proposal.discount && proposal.discount.upfront ? proposal.discount.upfront : '' %>">
                </div>
                <div style="display:flex; gap:4px; align-items:center;">
                    <label style="font-size:12px; color:#64748b; font-weight:600;">Annual</label>
                    <input type="number" name="discountAnnual" min="0" max="100" step="1" placeholder="%" style="width:60px; padding:6px 8px; border:2px solid #e2e8f0; border-radius:6px; font-size:13px; text-align:center;" title="Annual fee discount %" value="<%= proposal && proposal.discount && proposal.discount.annual ? proposal.discount.annual : '' %>">
                </div>
                <button type="submit" class="btn btn-sm" style="background: #8b5cf6;">
                    <%= proposal ? '🔄 Regenerate' : '⚡ Generate' %>
                </button>
            </form>
            <% } %>
            <% if (proposal) { %>
                <div style="display:flex; gap:8px; align-items:center; margin-left:auto;">
                    <% if (proposal.published) { %>
                        <form method="POST" action="/admin/projects/<%= encodeId(project.id) %>/proposal/unpublish" style="margin:0;">
                            <button type="submit" class="btn btn-sm" style="background: #ef4444;">🔒 Unpublish</button>
                        </form>
                    <% } else { %>
                        <form method="POST" action="/admin/projects/<%= encodeId(project.id) %>/proposal/publish" style="margin:0;">
                            <button type="submit" class="btn btn-sm" style="background: #10b981;">📤 Publish</button>
                        </form>
                    <% } %>
                    <form method="POST" action="/admin/projects/<%= encodeId(project.id) %>/proposal/delete" style="margin:0;" onsubmit="return confirm('Delete all proposals for this project? This cannot be undone.')">
                        <button type="submit" class="btn btn-sm" style="background: #dc2626;">🗑️ Delete</button>
                    </form>
                </div>
            <% } %>
        </div>
    </div>

    <% if (!design) { %>
        <div class="card" style="text-align: center; padding: 60px;">
            <div style="font-size: 3rem; margin-bottom: 16px;">📐</div>
            <h3 style="color: #64748b; margin-bottom: 8px;">Design Required First</h3>
            <p style="color: #94a3b8;">Extract a design before generating a proposal.</p>
            <a href="/admin/projects/<%= encodeId(project.id) %>" class="btn" style="margin-top: 16px;">← Back to Project</a>
        </div>
    <% } else if (locals.generating) { %>
        <div class="card" style="text-align: center; padding: 60px;" id="genCard">
            <div style="font-size: 3rem; margin-bottom: 16px;" id="genSpinner">⏳</div>
            <h3 style="color: #8b5cf6; margin-bottom: 8px;" id="genTitle">Generating Proposal...</h3>
            <p style="color: #94a3b8;" id="genMsg">AI is analysing your design and building a value-based proposal. This usually takes 15-30 seconds.</p>
            <div style="margin-top: 16px; width: 200px; height: 4px; background: #e2e8f0; border-radius: 2px; margin-left: auto; margin-right: auto; overflow: hidden;">
                <div style="width: 30%; height: 100%; background: #8b5cf6; border-radius: 2px; animation: progress 2s ease-in-out infinite;" id="genBar"></div>
            </div>
        </div>
        <style>@keyframes progress { 0% { width: 10%; margin-left: 0; } 50% { width: 60%; margin-left: 20%; } 100% { width: 10%; margin-left: 90%; } }</style>
    <% } else if (!proposal) { %>
        <div class="card" style="text-align: center; padding: 60px;">
            <div style="font-size: 3rem; margin-bottom: 16px;">💰</div>
            <h3 style="color: #64748b; margin-bottom: 8px;">No Proposal Yet</h3>
            <p style="color: #94a3b8; margin-bottom: 20px;">Generate a value-based fee proposal from the project design.</p>
            <form method="POST" action="/admin/projects/<%= encodeId(project.id) %>/generate-proposal" id="genForm2">
                <button type="submit" class="btn" style="background: #8b5cf6;">⚡ Generate Proposal</button>
            </form>
        </div>
    <% } else { %>
        <!-- Header -->
        <div class="card">
            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px;">
                <div>
                    <h3 style="margin-bottom: 4px;"><%= proposal.projectName || project.name %></h3>
                    <div style="color: #64748b; font-size: 0.9rem;">
                        Client: <strong><%= proposal.clientCompany || 'TBD' %></strong>
                        &nbsp;·&nbsp; Generated: <%= melbDate(proposal.createdAt) %>
                        &nbsp;·&nbsp; All prices ex GST
                    </div>
                </div>
                <div style="display:flex;gap:8px;align-items:center;">
                    <% if (proposal.approvedAt) { %>
                        <span style="background:#d1fae5;color:#065f46;padding:6px 12px;border-radius:20px;font-size:12px;font-weight:600;">✅ Approved by <%= proposal.approvedBy %> · <%= melbDate(proposal.approvedAt) %></span>
                    <% } else if (proposal.published) { %>
                        <span style="background:#dbeafe;color:#1e40af;padding:6px 12px;border-radius:20px;font-size:12px;font-weight:600;">📤 Published <%= proposal.publishedAt ? melbDate(proposal.publishedAt) : '' %></span>
                    <% } else { %>
                        <span style="background:#fef3c7;color:#92400e;padding:6px 12px;border-radius:20px;font-size:12px;font-weight:600;">Draft</span>
                    <% } %>
                </div>
            </div>

            <% if (proposal.commercialContext) { %>
            <div class="section">
                <h3>🏢 Commercial Context</h3>
                <p><%= proposal.commercialContext %></p>
            </div>
            <% } %>
        </div>

        <!-- Labour Analysis -->
        <% if (proposal.labourAnalysis) { %>
        <div class="value-card">
            <h3>📊 Current Cost Analysis</h3>
            <% if (proposal.labourAnalysis.currentProcess) { %>
            <div class="value-row">
                <span class="value-label">Current Process</span>
                <span class="value-text"><%= proposal.labourAnalysis.currentProcess %></span>
            </div>
            <% } %>
            <% if (proposal.labourAnalysis.hoursPerWeek) { %>
            <div class="value-row">
                <span class="value-label">Hours/Week</span>
                <span class="value-text"><%= proposal.labourAnalysis.hoursPerWeek %> hrs @ $<%= (proposal.labourAnalysis.hourlyRate || 0).toLocaleString() %>/hr</span>
            </div>
            <% } %>
            <% 
            // Calculate annual labour cost: if value looks wrong (< hourlyRate), compute from hours
            let calcLabourCost = proposal.labourAnalysis.annualLabourCost || 0;
            if (calcLabourCost < (proposal.labourAnalysis.hourlyRate || 100) && proposal.labourAnalysis.hoursPerWeek && proposal.labourAnalysis.hourlyRate) {
              calcLabourCost = proposal.labourAnalysis.hoursPerWeek * proposal.labourAnalysis.hourlyRate * 52;
            }
            %>
            <div class="value-row">
                <span class="value-label">Annual Labour Cost</span>
                <span class="value-text" style="font-weight:700;">$<%= calcLabourCost.toLocaleString() %></span>
            </div>
            <% if (proposal.labourAnalysis.additionalCosts) { %>
            <div class="value-row">
                <span class="value-label">Additional Costs Replaced</span>
                <span class="value-text"><%= proposal.labourAnalysis.additionalCosts %></span>
            </div>
            <% } %>
            <div class="value-row" style="border-top: 2px solid #93c5fd; padding-top: 12px;">
                <span class="value-label" style="font-weight: 700;">Estimated Total Annual Savings</span>
                <span class="value-text" style="font-size: 1.2rem; font-weight: 800; color: #1e40af;">$<%= (proposal.labourAnalysis.totalAnnualSavings || 0).toLocaleString() %></span>
            </div>
        </div>
        <% } %>

        <!-- Discount Banner -->
        <% if (proposal.discount && (proposal.discount.upfront || proposal.discount.annual || proposal.discount.percentage)) { %>
        <div style="background: linear-gradient(135deg, #fef3c7, #fde68a); border: 2px solid #f59e0b; border-radius: 10px; padding: 12px 20px; margin-bottom: 16px; display: flex; align-items: center; gap: 12px;">
            <span style="font-size: 24px;">🏷️</span>
            <div>
                <strong style="color: #92400e; font-size: 16px;"><%= proposal.discount.upfront && proposal.discount.annual ? proposal.discount.upfront + '%/' + proposal.discount.annual + '% Discount (Upfront/Annual)' : (proposal.discount.upfront ? proposal.discount.upfront + '% Upfront Discount' : (proposal.discount.annual ? proposal.discount.annual + '% Annual Discount' : proposal.discount.percentage + '% Discount')) %></strong>
                <% if (proposal.discount.reason) { %><div style="color: #a16207; font-size: 13px;"><%= proposal.discount.reason %></div><% } %>
            </div>
        </div>
        <% } %>

<!-- Two Fee Cards -->
        <div class="fee-grid" style="grid-template-columns: 1fr 1fr;">
            <div class="fee-card highlight">
                <div class="fee-label">1. Upfront Fee</div>
                <%
                const headerPreDiscount = proposal.upfrontFee ? (proposal.upfrontFee.originalTotal || (proposal.upfrontFee.totalHours || 0) * (proposal.upfrontFee.hourlyRate || 400)) : 0;
                const headerTotal = (proposal.upfrontFee && proposal.upfrontFee.total) || proposal.totalUpfront || 0;
                const headerDiscounted = headerPreDiscount > 0 && Math.abs(headerPreDiscount - headerTotal) > 1;
                %>
                <% if (headerDiscounted) { %>
                    <div class="fee-amount"><span style="text-decoration: line-through; color: #94a3b8; font-size: 0.7em;">$<%= headerPreDiscount.toLocaleString() %></span> $<%= headerTotal.toLocaleString() %></div>
                <% } else { %>
                    <div class="fee-amount">$<%= headerTotal.toLocaleString() %></div>
                <% } %>
                <div class="fee-desc" style="text-align: left; margin-top: 12px;">
                    <% if (proposal.upfrontFee && proposal.upfrontFee.breakdown) { %>
                        <% proposal.upfrontFee.breakdown.forEach(item => { %>
                            <div style="display: flex; justify-content: space-between; padding: 4px 0; border-bottom: 1px solid #e2e8f0;">
                                <span><strong><%= item.phase %></strong> (<%= item.hours %>hrs)</span>
                                <span>$<%= (item.hours * (proposal.upfrontFee.hourlyRate || 400)).toLocaleString() %></span>
                            </div>
                            <div style="font-size: 0.8rem; color: #94a3b8; padding: 2px 0 6px;"><%= item.description %></div>
                        <% }) %>
                        <%
                        const upfrontPreDiscount = (proposal.upfrontFee.totalHours || 0) * (proposal.upfrontFee.hourlyRate || 400);
                        const upfrontHasDiscount = proposal.upfrontFee.originalTotal || (upfrontPreDiscount > 0 && Math.abs(upfrontPreDiscount - (proposal.upfrontFee.total || 0)) > 1);
                        %>
                        <div style="display: flex; justify-content: space-between; padding: 8px 0 0; font-weight: 700; border-top: 2px solid #667eea;">
                            <span>Total (<%= proposal.upfrontFee.totalHours || 0 %> hrs × $<%= (proposal.upfrontFee.hourlyRate || 400).toLocaleString() %>/hr)</span>
                            <span>$<%= upfrontPreDiscount.toLocaleString() %></span>
                        </div>
                        <% if (upfrontHasDiscount) { %>
                        <div style="display: flex; justify-content: space-between; padding: 6px 0 0; font-weight: 800; color: #059669; font-size: 1.1em;">
                            <span>After Discount</span>
                            <span>$<%= (proposal.upfrontFee.total || 0).toLocaleString() %></span>
                        </div>
                        <% } %>
                    <% } %>
                </div>
            </div>
            <div class="fee-card highlight" style="border-color: #10b981;">
                <div class="fee-label" style="color: #10b981;">2. Annual Fee</div>
                <% if (proposal.annualFee && proposal.annualFee.originalTotal && proposal.annualFee.originalTotal !== proposal.annualFee.total) { %>
                    <div class="fee-amount"><span style="text-decoration: line-through; color: #94a3b8; font-size: 0.7em;">$<%= proposal.annualFee.originalTotal.toLocaleString() %></span> $<%= (proposal.annualFee.total || 0).toLocaleString() %> <small>/yr</small></div>
                <% } else { %>
                    <div class="fee-amount">$<%= ((proposal.annualFee && proposal.annualFee.total) || proposal.annualMaintenance || 0).toLocaleString() %> <small>/yr</small></div>
                <% } %>
                <% if (proposal.annualFee) { %>
                    <div style="font-size: 0.85rem; color: #64748b; margin: 8px 0;"><%= proposal.annualFee.calculation %></div>
                    <div style="font-size: 0.9rem; color: #475569; background: #f0fdf4; padding: 8px 12px; border-radius: 6px; margin: 8px 0;">
                        $<%= (proposal.annualFee.monthlyEquivalent || 0).toLocaleString() %>/month
                    </div>
                    <div class="fee-desc" style="text-align: left; margin-top: 8px;">
                        <strong>Includes:</strong> <%= proposal.annualFee.includes %>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- Summary Bar -->
        <div class="total-bar">
            <div class="total-item">
                <h4>Upfront (one-off)</h4>
                <div class="amount">$<%= ((proposal.upfrontFee && proposal.upfrontFee.total) || proposal.totalUpfront || 0).toLocaleString() %></div>
            </div>
            <div class="total-item">
                <h4>Annual Fee</h4>
                <div class="amount">$<%= ((proposal.annualFee && proposal.annualFee.total) || proposal.annualMaintenance || 0).toLocaleString() %> <small>/yr</small></div>
            </div>
            <div class="total-item">
                <h4>Timeline</h4>
                <div class="amount" style="font-size: 1.2rem;"><%= proposal.timeline %></div>
            </div>
        </div>

        <!-- ROI -->
        <% const roi = proposal.roiIllustration || proposal.roi; %>
        <% if (roi) { %>
        <div class="roi-card">
            <h3>📈 ROI Illustration</h3>
            <div class="roi-grid">
                <% if (roi.annualSavings) { %>
                <div class="roi-stat">
                    <div class="label">Annual Savings</div>
                    <div class="val">$<%= (roi.annualSavings || 0).toLocaleString() %></div>
                </div>
                <% } %>
                <% if (roi.annualFee) { %>
                <div class="roi-stat">
                    <div class="label">Annual Fee</div>
                    <div class="val">$<%= (roi.annualFee || 0).toLocaleString() %></div>
                </div>
                <% } %>
                <% if (roi.netAnnualBenefit) { %>
                <div class="roi-stat">
                    <div class="label">Net Annual Benefit</div>
                    <div class="val">$<%= (roi.netAnnualBenefit || 0).toLocaleString() %></div>
                </div>
                <% } %>
                <% if (roi.roiMultiple) { %>
                <div class="roi-stat">
                    <div class="label">ROI Multiple</div>
                    <div class="val"><%= roi.roiMultiple %></div>
                </div>
                <% } %>
                <% if (roi.paybackOnUpfront || roi.paybackPeriod) { %>
                <div class="roi-stat">
                    <div class="label">Upfront Payback</div>
                    <div class="val"><%= roi.paybackOnUpfront || roi.paybackPeriod %></div>
                </div>
                <% } %>
            </div>
            <% if (roi.summary) { %>
            <div class="roi-summary"><%= roi.summary %></div>
            <% } %>
        </div>
        <% } %>

        <!-- Assumptions & Exclusions -->
        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
            <div class="card">
                <h3 style="margin-bottom: 12px;">📌 Assumptions</h3>
                <div class="tag-list">
                    <% (proposal.assumptions || []).forEach(a => { %>
                        <span class="tag"><%= a %></span>
                    <% }) %>
                </div>
            </div>
            <div class="card">
                <h3 style="margin-bottom: 12px;">🚫 Exclusions</h3>
                <div class="tag-list">
                    <% (proposal.exclusions || []).forEach(e => { %>
                        <span class="tag excl"><%= e %></span>
                    <% }) %>
                </div>
            </div>
        </div>

        <!-- Feedback Chat -->
        <div class="card">
            <h3 style="margin-bottom: 16px;">💬 Proposal Feedback</h3>
            <p style="color: #94a3b8; font-size: 0.85rem; margin-bottom: 16px;">Add feedback below. When you regenerate, all feedback will be incorporated.</p>
            <div id="chatMessages" style="max-height: 400px; overflow-y: auto; margin-bottom: 16px;">
                <% (proposal.chat || []).forEach(msg => { %>
                    <div style="margin-bottom: 12px; padding: 10px 14px; border-radius: 8px; <%= msg.from === 'ai' ? 'background: #f0f4ff; border-left: 3px solid #667eea;' : 'background: #f8fafc; border-left: 3px solid #10b981;' %>">
                        <div style="font-size: 11px; color: #94a3b8; margin-bottom: 4px;"><%= msg.from === 'ai' ? '🤖 AI' : '👤 ' + msg.from %> · <%= melb(msg.ts) %></div>
                        <div style="font-size: 14px; color: #334155; white-space: pre-wrap;"><%= msg.text %></div>
                    </div>
                <% }) %>
            </div>
            <div style="display: flex; gap: 8px;">
                <input type="text" id="chatInput" placeholder="Add feedback on pricing, scope, assumptions..." style="flex: 1; padding: 10px 14px; border: 2px solid #e2e8f0; border-radius: 8px; font-size: 14px; outline: none;" />
                <button id="chatSend" class="btn" style="background: #10b981;">Send</button>
            </div>
        </div>

        <div class="card" style="text-align: center; color: #94a3b8; font-size: 0.85rem;">
            Valid until: <%= proposal.validUntil || '30 days from generation' %>
            &nbsp;·&nbsp; Proposal ID: <%= proposal.id %>
            <div style="margin-top: 8px; font-weight: 600; color: #64748b;">All prices are in AUD and exclude GST.</div>
        </div>
    <% } %>
</div>

<script>
document.querySelectorAll('form[action*="generate-proposal"]').forEach(f => {
    f.addEventListener('submit', function() {
        const btn = this.querySelector('button[type="submit"]');
        setTimeout(() => { btn.disabled = true; btn.textContent = '⏳ Generating proposal...'; }, 50);
    });
});

// Chat send
const chatInput = document.getElementById('chatInput');
const chatSend = document.getElementById('chatSend');
if (chatInput && chatSend) {
    const sendMsg = async () => {
        const text = chatInput.value.trim();
        if (!text) return;
        chatSend.disabled = true;
        chatInput.disabled = true;
        try {
            const res = await fetch('/admin/projects/<%= encodeId(project.id) %>/proposal/chat', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ text })
            });
            if (!res.ok) throw new Error('Failed');
            const data = await res.json();
            // Append message to chat
            const container = document.getElementById('chatMessages');
            data.messages.forEach(msg => {
                const div = document.createElement('div');
                div.style.cssText = 'margin-bottom: 12px; padding: 10px 14px; border-radius: 8px; ' +
                    (msg.from === 'ai' ? 'background: #f0f4ff; border-left: 3px solid #667eea;' : 'background: #f8fafc; border-left: 3px solid #10b981;');
                div.innerHTML = `<div style="font-size:11px;color:#94a3b8;margin-bottom:4px;">${msg.from === 'ai' ? '🤖 AI' : '👤 ' + msg.from} · ${new Date(msg.ts).toLocaleString('en-AU', { timeZone: 'Australia/Melbourne' })}</div><div style="font-size:14px;color:#334155;white-space:pre-wrap;">${msg.text}</div>`;
                container.appendChild(div);
            });
            container.scrollTop = container.scrollHeight;
            chatInput.value = '';
        } catch(e) { alert('Failed to send: ' + e.message); }
        chatSend.disabled = false;
        chatInput.disabled = false;
        chatInput.focus();
    };
    chatSend.addEventListener('click', sendMsg);
    chatInput.addEventListener('keydown', e => { if (e.key === 'Enter') sendMsg(); });
}

// Poll for generation status if generating
<% if (locals.generating) { %>
(function pollStatus() {
    const projectId = '<%= encodeId(project.id) %>';
    let elapsed = 0;
    const interval = setInterval(async () => {
        elapsed += 2;
        try {
            const res = await fetch(`/admin/projects/${projectId}/generation-status`);
            const data = await res.json();
            if (data.status === 'done') {
                clearInterval(interval);
                window.location.reload();
            } else if (data.status === 'error') {
                clearInterval(interval);
                document.getElementById('genSpinner').textContent = '❌';
                document.getElementById('genTitle').textContent = 'Generation Failed';
                document.getElementById('genTitle').style.color = '#dc2626';
                document.getElementById('genMsg').textContent = data.error || 'Unknown error. Try again.';
                document.getElementById('genBar').style.display = 'none';
            } else if (elapsed > 120) {
                clearInterval(interval);
                document.getElementById('genMsg').textContent = 'Taking longer than expected. Try refreshing the page.';
            }
        } catch(e) { /* retry */ }
    }, 2000);
})();
<% } %>
</script>

</body>
</html>
